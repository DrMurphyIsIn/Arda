/-
  R4-R7 campaign, PHASE 5d (part 2): the 36-branch cell dispatch and THE HEAD-MERGE
  MONOTONICITY THEOREM.

  Generated by gen_r47cert_cells.py per the P5d-2 spec in P5_SEAM_DESIGN.md.
  * 36 adapter theorems `dispatch_*`: the CI-green cell certificates re-parameterized
    from degree shifts (u, v) to natural degrees (dA, dB) under the hubward ordering
    `dB <= dA` (Nat.cast_sub bookkeeping; uniform box (dA-1)/(dB-k-1) * 3/16).
  * `head_merge_le`: box data from the P5b/P5d-1 bounds + the 36-way dispatch into
    `head_merge_le_of_cellD` -- the head merge does not decrease Aobj on the certified
    family (balanced arms, capped tail, donor split, degree ordering).

  HONEST BOUNDARY: the hubward ordering hypothesis is S5's named residual (the
  anti-hubward direction needs the role-reversed rule); Step-level assembly is P5e.
  conjecture1_proved=False.

  Genuine proofs (no `sorry`).
-/
import Mathlib
import R3Cert.R47Mono
import R3Cert.R47CertC
import R3Cert.R47CertD

namespace R3Cert
namespace Step3

open RTree

set_option maxHeartbeats 4000000

theorem dispatch_dA0 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 3 / 23 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 1) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 0 5 0 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 0 0 sQ sr := by
  have e1 : (dA : ℝ) = 2 + ((dB - 2 : ℕ) : ℝ) + ((dA - dB : ℕ) : ℝ) := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = 2 + ((dB - 2 : ℕ) : ℝ) := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dB : ℝ) - 1) = (((dB - 2 : ℕ) : ℝ) + 1) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 1) * (3 / 16) := hS1
      _ = (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact directA0_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dA1 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 3 / 23 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 1) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 1 5 0 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 1 0 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dB : ℝ) - 1) = (((dB - 2 : ℕ) : ℝ) + 1) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 1) * (3 / 16) := hS1
      _ = (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dA1_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dA2 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 3 / 23 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 1) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 2 5 0 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 2 0 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dB : ℝ) - 1) = (((dB - 2 : ℕ) : ℝ) + 1) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 1) * (3 / 16) := hS1
      _ = (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dA2_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dA3 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 3 / 23 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 1) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 3 5 0 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 3 0 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dB : ℝ) - 1) = (((dB - 2 : ℕ) : ℝ) + 1) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 1) * (3 / 16) := hS1
      _ = (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dA3_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dA4 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 3 / 23 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 1) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 4 5 0 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 4 0 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dB : ℝ) - 1) = (((dB - 2 : ℕ) : ℝ) + 1) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 1) * (3 / 16) := hS1
      _ = (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dA4_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dA5 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 3 / 23 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 1) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 5 5 0 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 5 0 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dB : ℝ) - 1) = (((dB - 2 : ℕ) : ℝ) + 1) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 1) * (3 / 16) := hS1
      _ = (((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dA5_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT04 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 2) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 0 4 1 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 0 1 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 2) = ((dB - 2 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 2) * (3 / 16) := hS1
      _ = ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT04_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT14 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 2) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 1 4 1 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 1 1 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 2) = ((dB - 2 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 2) * (3 / 16) := hS1
      _ = ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT14_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT24 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 2) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 2 4 1 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 2 1 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 2) = ((dB - 2 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 2) * (3 / 16) := hS1
      _ = ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT24_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT34 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 2) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 3 4 1 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 3 1 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 2) = ((dB - 2 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 2) * (3 / 16) := hS1
      _ = ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT34_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT44 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 2) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 4 4 1 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 4 1 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 2) = ((dB - 2 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 2) * (3 / 16) := hS1
      _ = ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT44_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT54 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 2 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 2) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 5 4 1 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 5 1 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 2 : ℕ) : ℝ) + 2 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 2 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 2 : ℕ) : ℝ) + 1 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 2) = ((dB - 2 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 2) * (3 / 16) := hS1
      _ = ((dB - 2 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT54_cell ((dA - dB : ℕ) : ℝ) ((dB - 2 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT03 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 3 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 3) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 0 3 2 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 0 2 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 3 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 3) = ((dB - 3 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 3) * (3 / 16) := hS1
      _ = ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT03_cell ((dA - dB : ℕ) : ℝ) ((dB - 3 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT13 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 3 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 3) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 1 3 2 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 1 2 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 3 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 3) = ((dB - 3 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 3) * (3 / 16) := hS1
      _ = ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT13_cell ((dA - dB : ℕ) : ℝ) ((dB - 3 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT23 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 3 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 3) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 2 3 2 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 2 2 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 3 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 3) = ((dB - 3 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 3) * (3 / 16) := hS1
      _ = ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT23_cell ((dA - dB : ℕ) : ℝ) ((dB - 3 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT33 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 3 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 3) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 3 3 2 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 3 2 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 3 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 3) = ((dB - 3 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 3) * (3 / 16) := hS1
      _ = ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT33_cell ((dA - dB : ℕ) : ℝ) ((dB - 3 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT43 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 3 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 3) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 4 3 2 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 4 2 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 3 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 3) = ((dB - 3 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 3) * (3 / 16) := hS1
      _ = ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT43_cell ((dA - dB : ℕ) : ℝ) ((dB - 3 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT53 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 3 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 3) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 5 3 2 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 5 2 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 3 : ℕ) : ℝ) + 3 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 3 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 3 : ℕ) : ℝ) + 2 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 3) = ((dB - 3 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 3) * (3 / 16) := hS1
      _ = ((dB - 3 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT53_cell ((dA - dB : ℕ) : ℝ) ((dB - 3 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT02 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 4 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 4) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 0 2 3 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 0 3 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 4 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 4) = ((dB - 4 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 4) * (3 / 16) := hS1
      _ = ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT02_cell ((dA - dB : ℕ) : ℝ) ((dB - 4 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT12 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 4 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 4) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 1 2 3 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 1 3 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 4 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 4) = ((dB - 4 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 4) * (3 / 16) := hS1
      _ = ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT12_cell ((dA - dB : ℕ) : ℝ) ((dB - 4 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT22 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 4 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 4) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 2 2 3 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 2 3 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 4 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 4) = ((dB - 4 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 4) * (3 / 16) := hS1
      _ = ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT22_cell ((dA - dB : ℕ) : ℝ) ((dB - 4 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT32 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 4 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 4) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 3 2 3 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 3 3 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 4 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 4) = ((dB - 4 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 4) * (3 / 16) := hS1
      _ = ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT32_cell ((dA - dB : ℕ) : ℝ) ((dB - 4 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT42 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 4 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 4) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 4 2 3 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 4 3 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 4 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 4) = ((dB - 4 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 4) * (3 / 16) := hS1
      _ = ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT42_cell ((dA - dB : ℕ) : ℝ) ((dB - 4 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT52 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 4 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 4) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 5 2 3 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 5 3 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 4 : ℕ) : ℝ) + 4 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 4 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 4 : ℕ) : ℝ) + 3 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 4) = ((dB - 4 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 4) * (3 / 16) := hS1
      _ = ((dB - 4 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT52_cell ((dA - dB : ℕ) : ℝ) ((dB - 4 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT01 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 5 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 5) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 0 1 4 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 0 4 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 5 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 5) = ((dB - 5 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 5) * (3 / 16) := hS1
      _ = ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT01_cell ((dA - dB : ℕ) : ℝ) ((dB - 5 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT11 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 5 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 5) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 1 1 4 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 1 4 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 5 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 5) = ((dB - 5 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 5) * (3 / 16) := hS1
      _ = ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT11_cell ((dA - dB : ℕ) : ℝ) ((dB - 5 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT21 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 5 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 5) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 2 1 4 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 2 4 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 5 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 5) = ((dB - 5 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 5) * (3 / 16) := hS1
      _ = ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT21_cell ((dA - dB : ℕ) : ℝ) ((dB - 5 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT31 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 5 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 5) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 3 1 4 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 3 4 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 5 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 5) = ((dB - 5 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 5) * (3 / 16) := hS1
      _ = ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT31_cell ((dA - dB : ℕ) : ℝ) ((dB - 5 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT41 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 5 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 5) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 4 1 4 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 4 4 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 5 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 5) = ((dB - 5 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 5) * (3 / 16) := hS1
      _ = ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT41_cell ((dA - dB : ℕ) : ℝ) ((dB - 5 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT51 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 5 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 5) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 5 1 4 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 5 4 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 5 : ℕ) : ℝ) + 5 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 5 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 5 : ℕ) : ℝ) + 4 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 5) = ((dB - 5 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 5) * (3 / 16) := hS1
      _ = ((dB - 5 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT51_cell ((dA - dB : ℕ) : ℝ) ((dB - 5 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT00 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 6 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 6) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 0 0 5 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 0 5 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 6 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 6) = ((dB - 6 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 6) * (3 / 16) := hS1
      _ = ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT00_cell ((dA - dB : ℕ) : ℝ) ((dB - 6 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT10 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 6 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 6) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 1 0 5 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 1 5 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 6 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 6) = ((dB - 6 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 6) * (3 / 16) := hS1
      _ = ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT10_cell ((dA - dB : ℕ) : ℝ) ((dB - 6 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT20 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 6 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 6) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 2 0 5 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 2 5 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 6 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 6) = ((dB - 6 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 6) * (3 / 16) := hS1
      _ = ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT20_cell ((dA - dB : ℕ) : ℝ) ((dB - 6 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT30 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 6 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 6) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 3 0 5 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 3 5 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 6 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 6) = ((dB - 6 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 6) * (3 / 16) := hS1
      _ = ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT30_cell ((dA - dB : ℕ) : ℝ) ((dB - 6 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT40 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 6 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 6) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 4 0 5 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 4 5 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 6 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 6) = ((dB - 6 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 6) * (3 / 16) := hS1
      _ = ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT40_cell ((dA - dB : ℕ) : ℝ) ((dB - 6 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

theorem dispatch_dT50 (dA dB : ℕ) (sQ sr : ℝ)
    (hord : dB ≤ dA) (hdb : 6 ≤ dB)
    (hQ0 : 0 ≤ sQ) (hQ1 : sQ ≤ ((dA : ℝ) - 1) * (3 / 16))
    (hS0 : 0 ≤ sr) (hS1 : sr ≤ ((dB : ℝ) - 6) * (3 / 16)) :
    beforeD (dA : ℝ) (dB : ℝ) 5 0 5 sQ sr
      ≤ afterD (dA : ℝ) (dB : ℝ) 5 5 sQ sr := by
  have e1 : (dA : ℝ) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
    ring
  have e2 : (dB : ℝ) = ((dB - 6 : ℕ) : ℝ) + 6 := by
    push_cast [Nat.cast_sub hdb]
    ring
  have hu : (0 : ℝ) ≤ ((dA - dB : ℕ) : ℝ) := by positivity
  have hv : (0 : ℝ) ≤ ((dB - 6 : ℕ) : ℝ) := by positivity
  have hQ1' : sQ ≤ (((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5) * (3 / 16) := by
    have h : ((dA : ℝ) - 1) = ((dA - dB : ℕ) : ℝ) + ((dB - 6 : ℕ) : ℝ) + 5 := by
      push_cast [Nat.cast_sub hord, Nat.cast_sub hdb]
      ring
    rw [← h]
    exact hQ1
  have hS1' : sr ≤ ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by
    have h : ((dB : ℝ) - 6) = ((dB - 6 : ℕ) : ℝ) := by
      push_cast [Nat.cast_sub hdb]
      ring
    calc sr ≤ ((dB : ℝ) - 6) * (3 / 16) := hS1
      _ = ((dB - 6 : ℕ) : ℝ) * (3 / 16) := by rw [h]
  rw [e1, e2]
  exact dT50_cell ((dA - dB : ℕ) : ℝ) ((dB - 6 : ℕ) : ℝ) sQ sr hu hv hQ0 hQ1' hS0 hS1'

/-- **THE HEAD-MERGE MONOTONICITY THEOREM** on the certified family: balanced arms,
    capped tail, donor split, and the hubward degree ordering (S5; the anti-hubward
    direction is a named residual).  The merge does not decrease the objective. -/
theorem head_merge_le (armsA : List ℕ) (cA : ℕ) (armsB others : List ℕ) (cb k : ℕ)
    (rest : List Hub)
    (hcA : cA ≤ 5) (hcb : cb ≤ 5) (hk : k = 5 - cb)
    (hsplit : armsB.Perm (List.replicate k 5 ++ others))
    (hbalA : BalancedArms armsA) (hbalO : BalancedArms others)
    (hcap : Capped rest) (hB5 : 5 ≤ armsB.length)
    (hord : armsB.length + (tailU rest).length ≤ armsA.length) :
    Aobj (backboneU ((armsA, cA) :: (armsB, cb) :: rest))
      ≤ Aobj (backboneU ((armsA ++ List.replicate k 4 ++ others ++ [5], cA) :: rest)) := by
  apply head_merge_le_of_cellD armsA cA armsB others cb k rest hsplit
  have hlenB : armsB.length = k + others.length := by
    simpa [List.length_append, List.length_replicate] using hsplit.length_eq
  set dA : ℕ := armsA.length + 1 with hdA
  set dB : ℕ := armsB.length + (tailU rest).length + 1 with hdB
  have hordN : dB ≤ dA := by omega
  have hQ0 : 0 ≤ sigmaArms armsA := sum_zw_arms_nonneg armsA
  have hQ1 : sigmaArms armsA ≤ ((dA : ℝ) - 1) * (3 / 16) := by
    have h := sum_zw_arms_le armsA hbalA
    have hcast : ((dA : ℝ) - 1) = (armsA.length : ℝ) := by
      rw [hdA]
      push_cast
      ring
    rw [hcast]
    exact h
  have hS0 : 0 ≤ sigmaArms others + qSum (tailU rest) :=
    add_nonneg (sum_zw_arms_nonneg others) (qSum_tailU_nonneg rest)
  interval_cases cb <;> interval_cases cA
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT00 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT10 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT20 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT30 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT40 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT50 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT01 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT11 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT21 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT31 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT41 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT51 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT02 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT12 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT22 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT32 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT42 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT52 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT03 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT13 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT23 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT33 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT43 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT53 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT04 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT14 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT24 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT34 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT44 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT54 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA0 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : others ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor others hbalO hne
        have h2 := qSum_tailU_nonneg rest
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA1 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : others ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor others hbalO hne
        have h2 := qSum_tailU_nonneg rest
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA2 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : others ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor others hbalO hne
        have h2 := qSum_tailU_nonneg rest
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA3 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : others ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor others hbalO hne
        have h2 := qSum_tailU_nonneg rest
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA4 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : others ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor others hbalO hne
        have h2 := qSum_tailU_nonneg rest
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA5 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : others ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor others hbalO hne
        have h2 := qSum_tailU_nonneg rest
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1) = (others.length : ℝ) + ((tailU rest).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms others ≤ (others.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le others hbalO
        have h2 := qSum_tailU_le rest hcap
        rw [hcast]
        linarith)

end Step3
end R3Cert
