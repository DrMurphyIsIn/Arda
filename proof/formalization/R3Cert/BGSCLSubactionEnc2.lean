/-
  Enclosure atoms for the next `IsSubaction ρwit` cells (2026-09-03): degree-3 hub
  profiles with a leaf/deg-2 child.  Generated by Telperion `emit_log_combination`
  (routes: tangent, tight_hi).  Companion to `BGSCLSubactionEnc.lean`.  Each atom is a
  message-independent scalar log-combination folded to a single `log X` and discharged;
  kernel-checked against `R3Cert.BGSCLInduction`.  No `sorry`.  `conjecture1_proved = False`.
-/
import Mathlib
import R3Cert.BGSCLInduction

namespace R3Cert
namespace BGSCL

open Real

-- ===== F*-folding, TIGHT-HI route (Q=79/96>0, fold X>1): 2 * Real.log (3/2 : ℝ) + Real.log (4/3 : ℝ) − 5·FSTAR ≤ 79/1056 (FSTAR = log(621/64)/11) =====
-- Fold X = ∏ rᵢ^(cᵢ·11) · (621/64)^(-5) = 1073741824/521343783 (≈ 2.0596) > 1; goal ⟺ log X ≤ Q = 79/96.
-- Degree-1 tangent is TOO LOOSE (X−1 > Q) and the `tight` route needs Q<0;
-- TIGHT-HI: log X ≤ Q ⟺ X ≤ exp Q (Real.log_le_iff_le_exp), and a degree-4
-- Taylor LOWER bound on exp Q (Real.exp_bound) gives exp Q ≥ 18186940987/8153726976 ≥ X.
theorem deg3_deg2children_enc : 2 * Real.log (3/2 : ℝ) + Real.log (4/3 : ℝ) - (5 * FSTAR : ℝ) ≤ (79/1056 : ℝ) := by
  rw [FSTAR]
  have hXpos : (0 : ℝ) < (3/2 : ℝ) ^ (22 : ℕ) * (4/3 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (5 : ℕ))⁻¹) := by positivity
  have hsplit : Real.log ((3/2 : ℝ) ^ (22 : ℕ) * (4/3 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (5 : ℕ))⁻¹))
      = 22 * Real.log (3/2 : ℝ) + 11 * Real.log (4/3 : ℝ) - 5 * Real.log (621/64 : ℝ) := by
    rw [Real.log_mul (by positivity) (by positivity),
        Real.log_mul (by positivity) (by positivity),
        Real.log_inv, Real.log_pow, Real.log_pow, Real.log_pow]
    push_cast; ring
  have hx : |(79/96 : ℝ)| ≤ 1 := by rw [abs_of_nonneg (by norm_num)]; norm_num
  have hb := Real.exp_bound hx (n := 4) (by norm_num)
  have hexpge : (18186940987/8153726976 : ℝ) ≤ Real.exp (79/96 : ℝ) := by
    have hlo := (abs_le.mp hb).1
    norm_num [Finset.sum_range_succ, Nat.factorial, abs_of_nonneg] at hlo
    linarith
  have hXexp : (3/2 : ℝ) ^ (22 : ℕ) * (4/3 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (5 : ℕ))⁻¹) ≤ Real.exp (79/96 : ℝ) := by
    have hXle : (3/2 : ℝ) ^ (22 : ℕ) * (4/3 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (5 : ℕ))⁻¹) ≤ (18186940987/8153726976 : ℝ) := by norm_num
    linarith
  have hlogX : Real.log ((3/2 : ℝ) ^ (22 : ℕ) * (4/3 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (5 : ℕ))⁻¹)) ≤ (79/96 : ℝ) := by
    rw [Real.log_le_iff_le_exp hXpos]; exact hXexp
  rw [hsplit] at hlogX
  linarith

-- ===== F*-folding, TANGENT route (general k=2): 1·log(3/2) − 2·FSTAR ≤ -1/144 (FSTAR = log(621/64)/11) =====
-- Fold: 11·(1·log 3/2 − 2·FSTAR) = log((3/2)^11·((621/64)^2)⁻¹)
-- ≤ (3/2)^11·((621/64)^2)⁻¹ − 1  (Real.log_le_sub_one_of_pos); the fold − 1
-- ≤ -11/144 is a rational norm_num fact.  TIGHT AT THE TIE (no F* lower bound).
theorem log32_sub2fstar : Real.log (3/2 : ℝ) - (2 * FSTAR : ℝ) ≤ (-1/144 : ℝ) := by
  rw [FSTAR]
  have hpos : (0 : ℝ) < (3/2 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (2 : ℕ))⁻¹) := by positivity
  have hr := Real.log_le_sub_one_of_pos hpos
  have hsplit : Real.log ((3/2 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (2 : ℕ))⁻¹))
      = 11 * Real.log (3/2 : ℝ) - 2 * Real.log (621/64 : ℝ) := by
    rw [Real.log_mul (by positivity) (by positivity), Real.log_pow,
        Real.log_inv, Real.log_pow]
    push_cast; ring
  rw [hsplit] at hr
  have hnum : (3/2 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (2 : ℕ))⁻¹) - 1 ≤ -11/144 := by norm_num
  linarith

-- ===== F*-folding, TANGENT route (general k=2): 1·log(13/9) − 2·FSTAR ≤ -3/416 (FSTAR = log(621/64)/11) =====
-- Fold: 11·(1·log 13/9 − 2·FSTAR) = log((13/9)^11·((621/64)^2)⁻¹)
-- ≤ (13/9)^11·((621/64)^2)⁻¹ − 1  (Real.log_le_sub_one_of_pos); the fold − 1
-- ≤ -33/416 is a rational norm_num fact.  TIGHT AT THE TIE (no F* lower bound).
theorem log139_sub2fstar : Real.log (13/9 : ℝ) - (2 * FSTAR : ℝ) ≤ (-3/416 : ℝ) := by
  rw [FSTAR]
  have hpos : (0 : ℝ) < (13/9 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (2 : ℕ))⁻¹) := by positivity
  have hr := Real.log_le_sub_one_of_pos hpos
  have hsplit : Real.log ((13/9 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (2 : ℕ))⁻¹))
      = 11 * Real.log (13/9 : ℝ) - 2 * Real.log (621/64 : ℝ) := by
    rw [Real.log_mul (by positivity) (by positivity), Real.log_pow,
        Real.log_inv, Real.log_pow]
    push_cast; ring
  rw [hsplit] at hr
  have hnum : (13/9 : ℝ) ^ (11 : ℕ) * (((621/64 : ℝ) ^ (2 : ℕ))⁻¹) - 1 ≤ -33/416 := by norm_num
  linarith

end BGSCL
end R3Cert
