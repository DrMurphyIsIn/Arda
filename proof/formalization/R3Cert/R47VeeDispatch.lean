/-
  R4-R7 campaign, PHASE 5e (part 3, fifth file): the Vee and mirror merge
  MONOTONICITY THEOREMS -- both directions, dispatched through the same 36 adapters.

  Generated by gen_r47cert_cells.py.  Hubward: the up-block joins the absorber's
  degree/sigma; anti-hubward: the roles swap (cell (cb, cA), k' = 5 - cA) and the
  donor's movers include its up-tail.  Box data from the P5b/P5d bounds; ordering
  hypotheses per direction.  With OrderedStep this is the last analytic layer before
  step_mono.  conjecture1_proved=False.

  Genuine proofs (no `sorry`).
-/
import Mathlib
import R3Cert.R47VeeMono

namespace R3Cert
namespace Step3

open RTree

set_option maxHeartbeats 4000000

/-- **THE VEE MERGE MONOTONICITY THEOREM** (hubward at any rooting): balanced arms,
    capped tails both sides, donor split, hubward ordering. -/
theorem vee_merge_le (armsA : List ℕ) (cA : ℕ) (armsB othersD : List ℕ) (cb k : ℕ)
    (dn upc : List Hub)
    (hcA : cA ≤ 5) (hcb : cb ≤ 5) (hk : k = 5 - cb)
    (hsplit : armsB.Perm (List.replicate k 5 ++ othersD))
    (hbalA : BalancedArms armsA) (hbalO : BalancedArms othersD)
    (hcapD : Capped dn) (hcapU : Capped upc) (hB5 : 5 ≤ armsB.length)
    (hord : armsB.length + (tailU dn).length ≤ armsA.length + (tailU upc).length) :
    AobjV upc (armsA, cA) ((armsB, cb) :: dn)
      ≤ AobjV upc (armsA ++ List.replicate k 4 ++ othersD ++ [5], cA) dn := by
  apply vee_merge_le_of_cellD armsA cA armsB othersD cb k dn upc hsplit
  have hlenB : armsB.length = k + othersD.length := by
    simpa [List.length_append, List.length_replicate] using hsplit.length_eq
  set dA : ℕ := armsA.length + (tailU upc).length + 1 with hdA
  set dB : ℕ := armsB.length + (tailU dn).length + 1 with hdB
  have hordN : dB ≤ dA := by omega
  have hQ0 : 0 ≤ sigmaArms armsA + qSum (tailU upc) :=
    add_nonneg (sum_zw_arms_nonneg armsA) (qSum_tailU_nonneg upc)
  have hQ1 : sigmaArms armsA + qSum (tailU upc) ≤ ((dA : ℝ) - 1) * (3 / 16) := by
    have h1 : sigmaArms armsA ≤ (armsA.length : ℝ) * (3 / 16) :=
      sum_zw_arms_le armsA hbalA
    have h2 := qSum_tailU_le upc hcapU
    have hcast : ((dA : ℝ) - 1)
        = (armsA.length : ℝ) + ((tailU upc).length : ℝ) := by
      rw [hdA]
      push_cast
      ring
    rw [hcast]
    linarith
  have hS0 : 0 ≤ sigmaArms othersD + qSum (tailU dn) :=
    add_nonneg (sum_zw_arms_nonneg othersD) (qSum_tailU_nonneg dn)
  interval_cases cb <;> interval_cases cA
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT00 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT10 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT20 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT30 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT40 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT50 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT01 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT11 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT21 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT31 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT41 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT51 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT02 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT12 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT22 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT32 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT42 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT52 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT03 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT13 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT23 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT33 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT43 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT53 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT04 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT14 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT24 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT34 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT44 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT54 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA0 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg dn
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA1 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg dn
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA2 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg dn
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA3 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg dn
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA4 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg dn
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA5 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg dn
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU dn).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le dn hcapD
        rw [hcast]
        linarith)

/-- **THE MIRROR MERGE MONOTONICITY THEOREM** (anti-hubward): the donor heads the
    up-chain; same table at the role-swapped instantiation. -/
theorem mirror_merge_le (armsA othersD : List ℕ) (cA : ℕ) (armsB : List ℕ)
    (cb k : ℕ) (dn upc : List Hub)
    (hcA : cA ≤ 5) (hcb : cb ≤ 5) (hk : k = 5 - cA)
    (hsplit : armsA.Perm (List.replicate k 5 ++ othersD))
    (hbalB : BalancedArms armsB) (hbalO : BalancedArms othersD)
    (hcapD : Capped dn) (hcapU : Capped upc) (hA5 : 5 ≤ armsA.length)
    (hord : armsA.length + (tailU upc).length ≤ armsB.length + (tailU dn).length) :
    AobjV ((armsA, cA) :: upc) (armsB, cb) dn
      ≤ AobjV upc (armsB ++ List.replicate k 4 ++ othersD ++ [5], cb) dn := by
  apply mirror_merge_le_of_cellD armsA othersD cA armsB cb k dn upc hsplit
  have hlenB : armsA.length = k + othersD.length := by
    simpa [List.length_append, List.length_replicate] using hsplit.length_eq
  set dA : ℕ := armsB.length + (tailU dn).length + 1 with hdA
  set dB : ℕ := armsA.length + (tailU upc).length + 1 with hdB
  have hordN : dB ≤ dA := by omega
  have hQ0 : 0 ≤ sigmaArms armsB + qSum (tailU dn) :=
    add_nonneg (sum_zw_arms_nonneg armsB) (qSum_tailU_nonneg dn)
  have hQ1 : sigmaArms armsB + qSum (tailU dn) ≤ ((dA : ℝ) - 1) * (3 / 16) := by
    have h1 : sigmaArms armsB ≤ (armsB.length : ℝ) * (3 / 16) :=
      sum_zw_arms_le armsB hbalB
    have h2 := qSum_tailU_le dn hcapD
    have hcast : ((dA : ℝ) - 1)
        = (armsB.length : ℝ) + ((tailU dn).length : ℝ) := by
      rw [hdA]
      push_cast
      ring
    rw [hcast]
    linarith
  have hS0 : 0 ≤ sigmaArms othersD + qSum (tailU upc) :=
    add_nonneg (sum_zw_arms_nonneg othersD) (qSum_tailU_nonneg upc)
  interval_cases cA <;> interval_cases cb
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT00 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT10 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT20 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT30 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT40 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 5 := by omega
    exact dispatch_dT50 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 6)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT01 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT11 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT21 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT31 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT41 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 4 := by omega
    exact dispatch_dT51 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 5)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT02 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT12 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT22 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT32 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT42 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 3 := by omega
    exact dispatch_dT52 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 4)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT03 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT13 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT23 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT33 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT43 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 2 := by omega
    exact dispatch_dT53 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 3)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT04 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT14 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT24 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT34 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT44 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 1 := by omega
    exact dispatch_dT54 dA dB _ _ hordN (by omega) hQ0 hQ1 hS0
      (by
        have hcast : ((dB : ℝ) - 2)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA0 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg upc
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA1 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg upc
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA2 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg upc
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA3 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg upc
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA4 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg upc
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)
  · obtain rfl : k = 0 := by omega
    exact dispatch_dA5 dA dB _ _ hordN (by omega) hQ0 hQ1 (by
        have hne : othersD ≠ [] := by
          intro h0
          rw [h0] at hlenB
          simp only [List.length_nil] at hlenB
          omega
        have h1 := sigmaArms_floor othersD hbalO hne
        have h2 := qSum_tailU_nonneg upc
        linarith)
      (by
        have hcast : ((dB : ℝ) - 1)
            = (othersD.length : ℝ) + ((tailU upc).length : ℝ) := by
          rw [hdB, hlenB]
          push_cast
          ring
        have h1 : sigmaArms othersD ≤ (othersD.length : ℝ) * (3 / 16) :=
          sum_zw_arms_le othersD hbalO
        have h2 := qSum_tailU_le upc hcapU
        rw [hcast]
        linarith)

end Step3
end R3Cert
