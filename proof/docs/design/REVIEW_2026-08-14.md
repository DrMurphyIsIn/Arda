# Independent adversarial review — R3Cert bridge (2026-08-14)

Reviewer: independent session (read-only; no `lake`/`lean` run locally per hardware
constraint). Worktree `/Users/peterwmurphy/arda-wt-edit`, branch
`experiment/laplacian-fischer-cavity`, HEAD `32b2d4d4`. Scope: STATEMENTS and
DEFINITIONS of the machine-checked chain (the kernel checked the proofs in CI). I read
the `.lean` sources fresh and did the hand-computed spot checks from the definitions as
written, not from docstrings.

**Headline:** this is a materially stronger artifact than the 2026-08-09 Lean audit
described. The `Prop := True` placeholder that audit flagged as the H2 core bridge
(`pi_is_cavity_recursion`) is **gone** — it does not exist anywhere in the tree. Both
the potential/`Φ≤1` chain and the permanent-of-Laplacian bridge are now genuine,
fully-quantified theorems with real proofs. The one honestly-disclaimed residual is
the R4–R7 reduction layer (near-star competitor restriction), which stays at
Python/paper level, and `conjecture1_proved=False` is stated consistently across all
Bridge files (independent review + reduction layer still open).

---

## Verdict per numbered item

**1. `Reach.lean` DEC recursion definitions — PASS.**
`Branch`, `cav`, `cavSum`, `zc`, `ac`, `eroot`, `logPhi` match the DEC recursion exactly.
`cav (node c ch) = 3/(3 + 3·nch + 4c + 3S)` (line 30); `zc c nch = 3/(3(nch+1+c)+c)`
(line 160, i.e. `z=3/(3d+c)`, `d=nch+1+c`); `ac c nch = (3/2)^c·(1+c/(3d))/rhoB^(1+2c)`
(line 163); `eroot = log(ac) + log(1 + z·S)` (line 167); `logPhi (node c ch) =
logPhiSum ch + eroot` (line 172). `rhoB` is the exact `(621/64)^(1/11)` object used
throughout. All hand checks below confirm these are self-consistent.

**2. `phi_le_one` — PASS (unconditional, for the Branch cavity model).**
`PotentialFinal.lean:49` `theorem phi_le_one (b : Branch) : logPhi b ≤ 0` — a real
universal quantifier over EVERY `Branch`, no hidden hypothesis. It is discharged
unconditionally: `deficitNonneg_holds` (from `starBound_holds` ← `e1/e2/e3Bound` ←
`e2Bound_holds`) → `validPotentialPlain_holds` → `plainConjecture_holds` →
`phi_le_one_of_deficitNonneg`. Crucially this is NOT the conditional
`Reach.phi_le_one_of_potential` (which the docstring correctly still calls the "open
crux"); the explicit potential `Pval` is proved valid. The whole `DeficitNonneg`
family (`PotentialAssembly.lean:29`) is a genuine tree-free inequality
`∀ (a nl m : ℕ) (Sg∈[0,m/2])`, and its base `e2Bound_holds`
(`PotentialE2Small.lean:173`) covers the ENTIRE infinite family — main region
`4a+6nl≥10` via `e2_poly`'s SOS `nlinarith` with hint `(7a−36)²` plus m-monotonicity,
and the small corner `{(0,0),(1,0),(2,0),(0,1)}` by exact small-m cases + m-tails. No
`native_decide`, no finite truncation.

**3. `BridgeStep4c` (raw-amplitude seam) — PASS.**
`dB (node c ch) = ch.length+1+c` (the DEC `d`, line 42); `Vb (node c ch) = 1+2c+VbSum`
(literal vertex count, line 52). `litRealize` expands each cherry as a genuine 2-path
(`cherryMid` degree-2 mid + degree-1 leaf, weight `1/2`; root-mid weight `1/(d·2)`) and
carries raw weights `1/(deg u · deg v)` (lines 83–90). `Ztot_lit_eq_Wb` (line 293) is a
real rational identity via `Matched_factor`. `exp_logPhi_mul_rhoB_pow`
(line 361): `exp(logPhi b)·rhoB^(Vb b) = Ztot (litRealize b)` — genuine, finite, local.
Conventions match `raw_amplitude_seam.py` S1/S2 (weight = reciprocal degree product,
degree includes the phantom parent edge in `dB`).

**4. `BridgeStep4d` (hub limit) — PASS.**
`litHub c ch` uses the TRUE root degree `D = c + ch.length` (no phantom `+1`, line 39) —
distinct from `dB`, correctly. `Ztot_litHub` (line 49) is the exact closed form.
`amplitude_bridge` (line 100) and `amplitude_bridge_logPhi` (line 177) are genuine
`Tendsto` limits over `p` (arm count); the shared `(3/2)^cH·WbProd(arms)` factor
cancels at every finite `p` and the hub load `(pm+S)/(p+E)→m`. The limit constant
`exp(logPhi b)·rhoB^(Vb b)` does not depend on `p`; the graphs genuinely do
(Check D satisfied).

**5. `Matching.lean` + `BridgeStep3d` — PASS.**
`lapl` (Matching.lean:62) is the real Laplacian: diagonal `deg i`, `−1` on `Adj`, `0`
else (`lapl_diag`, `lapl_adj` confirm). `Matrix.permanent` is Mathlib's, summed over ALL
of `Perm V`. `IsEdgeEnum` (BridgeStep3d:36) is the honest "each edge once, weight
`1/(deg·deg)`, complete, unique" structure. `pi_eq_msum` (line 412):
`per(lapl G)/∏deg = msum E` under `IsAcyclic` + `∀v, deg v ≠ 0` (both discharged for
the concrete graph in 4i/4j). The involution↔matching bijection is fully proved
(`toPerm`, `filter_toPerm`, `toPerm_filter`, weight identity), resting on
`acyclicForcesInvolution` — now a THEOREM (Involution.lean), not an assumption.

**6. `BridgeStep3e/3f/4e/4f/4g/4h` — PASS.**
`aGraph E` (3e:61) is a genuine `SimpleGraph` on the vertex-support subtype `AVert E`
(all touched addresses); adjacency = distinct + recorded unordered key. `liftEdges`,
`msum_liftEdges` (msum invariant under the subtype relabeling) are real. `realize_shape/
nodup/hloop/keys` (3f:164–182) are genuine theorems (edge = parent→index-consed child;
key uniqueness by childNodup + a length argument ruling out self-reversal). `GoodTree`
+ `litHub_good`/`litRealize_good` (4g) encode the weight convention with
`childCount(litRealize K)+1 = dB K`. `realize_weights` (4h:146) proves every edge
carries `1/(count(parent)·count(child))` by mutual induction, and `degree_eq_card_
touching` (4e) + `card_touching_eq_countP` (4f) tie degree = touching count = the
weight denominator. Degree convention verified.

**7. `BridgeStep4i/4j` (THE CAPSTONES) — PASS.**
`pi_litHub'` (4j:103) and `amplitude_bridge_real'` (4j:111) are **unconditional**. The
graph is `aGraph (realize (litHub c ch))` — the real competitor tree; the LHS is a
genuine Laplacian-permanent ratio over ALL `v : AVert` of that graph; the limit is
`exp(logPhi b)·rhoB^(Vb b)`. `aGraph_realize_isAcyclic` (4j:43) is genuinely proved
(no hidden hypothesis): maximal-length address on a cycle, both cycle-neighbours descend
from it, a descending edge determines the other endpoint, so the two neighbours
coincide — contradicting `IsCycle.snd_ne_penultimate`. NOTE: 4i still TAKES acyclicity
as a hypothesis and its docstring says so; 4j discharges it and states the
unconditional forms. `aGraph_degree_pos` (4i) proves degree positivity (the `hpos` in
`pi_eq_msum`). The 4i header ("acyclicity remains a hypothesis") is stale relative to
4j but not false — see Concern C1.

---

## Stub scan at HEAD (Mandatory Check A)

Scanned every `R3Cert/*.lean` (excluding `.lake/`) for `sorry`, `admit`,
`native_decide`, `Prop := True`, `:= True`, `axiom`, `partial def`, `unsafe`,
`@[implemented_by]`.

- `sorry`: **no code hits.** All ~45 hits are docstrings asserting "no `sorry`".
- `admit`, `native_decide`, `unsafe`, `@[implemented_by]`, `partial def`: **zero hits.**
- `axiom`: **zero declarations.**
- `Prop := True` / `:= True` bodies: the ONLY `=> True` body is
  `Plainify.lean:186 IsPlainList [] => True` — the correct vacuous base case of the
  plain-list predicate (the empty list is trivially plain). Legitimate, not a stub.
- The 2026-08-09-flagged placeholder `pi_is_cavity_recursion` (`Prop := True`):
  **does not exist** in the tree. `near_star_tie` (previously "orphaned/decorative")
  is now a real integer identity `ExactCruxes.lean:48` `64·243·23 = 621·576` (verified
  357696 = 357696).
- All `def _ : Prop :=` occurrences are genuine mathematical predicates
  (`DeficitNonneg`, `StarBound`, `E1/2/3Bound`, `ValidPotential(Plain)`,
  `permanent_is_matching_sum`, etc.), each subsequently PROVED by a `theorem …_holds`.

## Hand-computed spot checks (Mandatory Check B), from the definitions

- `cav (node 0 [])` (bare leaf): `3/(3+0+0+0) = 1`. ✓ (matches `E0_holds` bare-leaf branch)
- `cav (node 0 [node 0 []])` (arm): child leaf cav 1, S=1, len=1 → `3/(3+3·1+0+3·1)=3/9=1/3`. ✓
  (matches `cav_arm`)
- `Ztot (litRealize (node 1 []))`: `litRealize = node [litCherry 2]`; Popen = 3/2,
  Matched = (1/(2·2))·1·1 = 1/4 → Ztot = 7/4. ✓
- `Wb (node 1 [])`: `(3/2)·(1+1/(3·2))·(1+0)·1 = (3/2)(7/6) = 7/4`. ✓ (= Ztot, so
  `Ztot_lit_eq_Wb` holds here)
- `Vb (node 1 [])`: `1+2·1+0 = 3`. ✓
- `childCount (litRealize (node 1 [])) + 1 = 2 = dB (node 1 [])`. ✓
- `lapl` diagonal = `deg`, off-diagonal `−1` exactly on `Adj` (from `lapl_diag`/`lapl_adj`). ✓

## Degenerate instantiations (Check C)

`litHub 0 [] = RTree.node []` → `realize = []` → `AVert []` empty → `aGraph []` empty.
`pi_litHub' 0 []` then reads `(empty permanent)/(empty product) = 1 = Ztot(node []) = 1`,
i.e. `1 = 1`. Literally true, harmless; the interesting instances (`p ≥ 1`, nonempty
`ch`) are nonempty. Not a soundness problem.

## Import DAG (Check E)

`Reach`, `Plainify`, and the entire `Potential*` chain import NO Bridge/Matching/
CavityTree file. So `phi_le_one` cannot be circular with the permanent-of-Laplacian
bridge; the two halves are independent modules joined only at the very top (R3Cert.lean
root aggregates both). No circularity.

## Ledger honesty (Check F)

`conjecture1_proved=False` is stated in every Bridge file and the crux-adjacent files
(`BushBound`, `Locality`, `ChainMargin`). 4i explicitly: "acyclicity lemma +
independent review remain"; 4j: "review pending". PotentialFinal states its HONEST
SCOPE (Branch cavity model; H2 bridge separate). No file claims the conjecture is fully
proved. Honest.

---

## Concerns (non-blocking)

- **C1 (stale docstring, not a math gap).** `BridgeStep4i.lean:16–21` still says
  acyclicity "is taken as a hypothesis here, NOT asserted." That was true at 4i but is
  superseded by `aGraph_realize_isAcyclic` in 4j, which makes `pi_litHub'` /
  `amplitude_bridge_real'` unconditional. The 4i statement is not false (4i's own
  theorems really are conditional); the header just reads as more pessimistic than the
  final state. Recommend a one-line pointer to 4j.
- **C2 (docstring lag).** `Plainify.lean:11–12` and several Potential-chain headers
  still call the plain-tree bound "the open Brualdi–Goldwasser crux
  (`Reach.ValidPotential` existence)." As of `PotentialFinal` that crux is CLOSED for
  the Branch model. The statements are correct; only the prose is out of date.
- **C3 (`nlinarith`/SOS opacity).** The base inequalities (`e2_poly`, the E2Small
  corner cases) close via `nlinarith` with explicit SOS/product hints. The kernel
  checked them (CI-green), and I confirmed the constant budgets
  (`10441/1510441 ≤ −ω`, `229/1229 ≤ L`) and the tie arithmetic by hand, so this is
  not a defect — only a note that these steps are not human-legible from the source
  alone; they rest on the kernel.

## What the machine-checked results do NOT establish (scope honesty)

- **The R4–R7 reduction layer is NOT formalized.** The Lean chain proves `Φ ≤ 1` for
  the DEC `Branch` cavity model and bridges that model to `per L(G)/∏deg` for the
  literal-hub competitor family `aGraph (realize (litHub c ch))`. The reduction of the
  ORIGINAL Brualdi–Goldwasser maximizer problem to this cavity model / near-star
  competitor family (the "R4–R7" layer, the near-star restriction) remains at
  Python/paper level, as the docstrings state. The near-star tie `Φ=1` at `k=5` lives
  inside the model but the claim that these competitors are the extremal ones is not in
  Lean.
- **The bridge limit is over the hub arm-count `p`, not over all trees.** The
  capstones establish the amplitude of a single gadget branch `b` as a limit of hub
  ratios; assembling these into a statement about the global maximizer is the
  reduction layer above, not proved here.
- **`conjecture1_proved` is correctly `False`.** Full closure needs (i) the reduction
  layer and (ii) independent review — both explicitly pending.
- **The `nlinarith`-closed base inequalities are trusted via the kernel**, not
  re-derived here (C3).

## Overall verdict

**PASS-WITH-CONCERNS.** The strongest claim the formalization genuinely supports, in
one sentence: *within the DEC `Branch` cavity model, `Φ ≤ 1` holds unconditionally for
every tree (no `sorry`/axiom/`native_decide`, full universal quantifier, explicit
valid potential), and the finite Laplacian-permanent ratio `per L(G)/∏deg` of the
realized literal-hub competitor graphs converges to that model's amplitude
`exp(logPhi b)·rhoB^(Vb b)` — so the two halves of the bridge are each machine-checked
and non-circular* — with the ONLY residual being the (honestly disclaimed, non-Lean)
R4–R7 reduction from the original maximizer problem to this competitor family, plus the
three docstring/opacity concerns above.
