# MR !69 hand-off: the 2026-08-14 arc (K-boundary -> Stage II closed)

For the live session integrating this MR into `experiment/laplacian-fischer-cavity`.
18 commits, 15 self-verifying Python modules, 3 design docs.  Everything below is on branch
`review/kelmans-boundary` (worktree `~/arda-wt-kboundary`), pushed.  `conjecture1_proved=False`
everywhere, correctly: what remains is G1 hardening, your G7 Lean campaign, and review.

## Read in this order

1. `R7_ARCHITECTURE.md` -- the composed proof skeleton (stages I-IV) and the gap table with
   per-gap status.  This is the map; everything else is a leaf of it.
2. `P2B_MERGE_DESIGN.md` -- what your P2b Step relation should adopt (the unified topped-up
   merge; no stuck states, no debris, one constructor).
3. `REVIEW_BRIEF_20260814.md` -- ready-to-dispatch adversarial review brief for this arc.
4. Module docstrings as needed (below).

## One-command verification

    .venv python3 proof/verify.py

runs every module's `run_all()` (approx. 20-40 min; the 442k-case sweep dominates).  Every
claim in every module is an assert; the script fails loudly on any regression.

## Module inventory (chronological = dependency order)

| module | result | rigor |
|---|---|---|
| kelmans_mixed_load.py | per-vertex-load model + exact bilinear Kelmans identity; two-hub exchange DICHOTOMY (donor loaded => merge up, de-loaded => provably down); 25/30-cell box theorem | symbolic + exact |
| kelmans_vertex_budget.py | two-hub stuck template-domination THEOREM; ASSISTED MERGE theorem (borrow-then-merge, fused) | symbolic |
| kelmans_env_rules.py | assisted merge in ENVIRONMENT (bilinear D-form); residual direct cells closed via the ONE-ARM FLOOR | symbolic |
| kelmans_unified_merge.py | THE UNIFIED TOPPED-UP MERGE: one debris-free rule, 36/36 cells at cap 3/16 | symbolic |
| rate_bound_fixed_n.py | pi(T) = Z(T^r)*R(r) EXACT; pi <= (4/3) rhoB^n for EVERY tree (via Lean phi_le_one) | exact + Lean input |
| slack_ledger_dichotomy.py | the hinge certificate telescoped into a DEFECT LEDGER; far/near dichotomy; pure-hub collapse localized at the Lemma-A locus | exact identity + grid floors |
| amortized_hub_bound.py | ledger >= 0.0235 * #pure-hubs (tax lemma + hub charge + charging scheme) | grid + analytic skeleton |
| gap_discharges.py | G2 (Plainify, zero loss -- ledger valid for ALL trees) + G5/G6 (de-loading schedule theorem, four shedding lemmas, n0 = 421) | exact + symbolic |
| g34_merge_unblocking.py | cap widening to 1/5 + (1,5) route; the BALANCING LEMMA (>= 2 arm-neighbours, sharp in kind); R <= 6/5 at cherry-tip roots | symbolic |
| g34_residual_domination.py | defected TWO-HUB stuck configs dominated at all sizes (tails + per-residue comparators + 442,800-case exact sweep) | symbolic + exact sweep |
| g34_multi_starofhubs.py | depth-2 star archetypes dominated: 972 symbolic certificates, zero hard cases + asymmetric sweep | symbolic + exact |
| g34_deep.py | generic THETA-lemma (ledger > 0.2813 => dominated, shape-blind); depth >= 4 generic; depth-3 finite + exact | composed + exact |
| interpolation_lemma.py | the EXACT sub-hub curve cav = 23/(26q+23), B = 26/(rhoB(23+3cav)); the sign dichotomy 23z-3-3Tz; ALL size vectors closed | exact identities + interval endpoints |
| kelmans_mixed_load/vertex_budget/... | (supporting census/probe functions inside the above) | -- |

## Integration guidance for the R47 campaign

* **P2b Step relation**: adopt the unified topped-up merge as the single hub-merge
  constructor (P2B_MERGE_DESIGN.md).  The tex's (H)/(K) split and the stuck-state escape
  hatch are obsolete; rem:import's strictness story is superseded by the dichotomy + assisted
  merge.  mu drops by one hub per merge; termination is immediate.
* **Stratum-(i) phase**: the rate identity (rate_bound_fixed_n) ports in ~1 CI file on top of
  BridgeStep4c + phi_le_one (port notes in the module docstring).  Suggest doing this early:
  it machine-checks that the campaign's objective bounds the real quantity for every tree.
* **P4/P5 discharges**: every certificate in this MR is a rational polynomial inequality with
  an explicit all-nonneg witness (positivity-friendly).  The 36-cell merge table and the four
  shedding lemmas are the natural first ports.
* **The ledger layer** (slack_ledger onward) formalizes AFTER the potential chain: it is the
  same hinge algebra telescoped; the floors are 1-var piecewise minimizations (G1).

## Footguns and honest boundaries

* NO local `lake build` on this machine (SoC watchdog); Lean verification via GitLab CI only.
* Exit-code masking: `python ... | tail` hides assertion failures -- check output text.
* The `(0,1,0)` plain shape is the folded ARM, excluded from structural-node accounting.
* Comparator selection matters: the defect-carrying template GENUINELY loses at small
  odd-residue sizes (cA=5 donor-leaf, ratio 0.969); the per-residue search is load-bearing.
* Interval-float layers (heavy-top endpoints, ledger floors) carry >= 2% guards but are
  G1-hardening targets, not yet symbolic.
* `conjecture1_status.py`'s R7 dict entry still predates this arc (noted in MR note 1);
  update it during integration.
* The balancing lemma is FALSE on bare 2-arm hubs; its >= 2-arm-neighbour condition is
  necessary, not an artifact.

## Suggested next actions (yours to sequence)

1. Integrate the MR (linear history or merge; no conflicts expected -- all files are new
   except R7_ARCHITECTURE.md, P2B_MERGE_DESIGN.md which are also new).
2. Dispatch the independent review (REVIEW_BRIEF_20260814.md).
3. Update conjecture1_status.py: R7 entry + the new stage statuses.
4. Continue the R47 campaign against the updated P2b design.
5. When ripe: reconcile the outreach tex (kelmans_confluence.tex's abstract/'what remains'
   are superseded) -- BEFORE any external use.
