# Verification-layer dependencies (exact certificates use only stdlib fractions;
# these support the symbolic/numeric harnesses, tree search, and figures).
sympy>=1.12
numpy>=1.26
networkx>=3.2
scipy>=1.11
mpmath>=1.3
matplotlib>=3.8

# Optional: independent PARI/GP cross-checks (independent_verification.py)
# cypari2>=2.1
