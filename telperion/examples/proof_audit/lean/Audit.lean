/- Compile roll-up for the audit's frozen re-derivations (files copied from
   frozen/ at CI time — frozen/ is the single source of truth). -/
import Audit.Shed
import Audit.Legs
import Audit.InterpI1
import Audit.InterpI2
import Audit.InterpLight
