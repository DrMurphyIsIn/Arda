# SubsetFormPSD d=2: the exact certificate (discovered + verified 2026-08-21)

Entrywise-exact identity (verified in Fractions for n = 9, 11, 13, 15
against the full 1+n+C(n,2) moment matrix; discovery symbolic in n):

    Q_2 = (A + f1*s1 + f2*s2)^2                       [level-0, rank one]
        + (n/(4(n-1))) * [ sum_i t_i^2 - (sum t)^2/n ] [level-1, centered CS]
        + mu2 * N2(z)                                  [level-2, projection]

with t_i = y_i + (4(n-1)/n) * (Cz)_i  (pseudo-inverse elimination;
C has entries f2 - f1 f2 incident / f3 - f1 f2 nonincident),
mu2 = n(n-2)/(16(n-3)(n-1)) > 0 for n > 3, and N2 the J(n,2) level-2
projection norm  N2 = P - (W - 4 s2^2/n)/(n-2) - 2 s2^2/(n(n-1)).

STRUCTURAL FINDINGS (each verified symbolically, sympy-exact):
1. The y-block J-eigenvalue after the A-square is THE TIE ZERO
   (f1 + (n-1) f2 - n f1^2 = 0): B is singular in the all-ones direction.
2. The cross block is kernel-compatible (n c_non + 2(c_inc - c_non) = 0):
   the pseudo-inverse Schur step is exact, no residue.
3. The z-residual scheme eigenvalues: mu0 = 0, mu1 = 0 -- ALL lower
   harmonics are annihilated exactly. The rank-one theory made manifest:
   each harmonic level of the moment matrix lives in exactly one
   certificate piece, and the tie saturates every lower level.

REMAINING FOR LEAN (the SymmetricQuadFormEmitter target): the three-piece
identity as a Finset computation (the d=1 pattern one level up), plus
N2 >= 0 -- either the explicit pair-harmonic sum-of-squares or the chained
inequality (n-2)(n(n-1)) P >= n(n-1)(W - 4 s2^2/n) + 2(n-2) s2^2.

Generated by gen_d2_decomposition.py (all discovery constants printed and
factored; ray positivity by inspection).
