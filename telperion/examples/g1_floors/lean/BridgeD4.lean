/- TELPERION-GENERATED by telperion/examples/knapsack_sos/gen_bridge_d4.py.
   DO NOT EDIT BY HAND -- regenerate instead.

   All 35 Gram-bridge entry identities for the degree-8 (d = 4) knapsack
   pseudoexpectation: the combinatorial Gram formula equals the rank-1 form
   g_k * v(i-k) * v(j-k) for every block k <= 4 and entry k <= j <= i <= 4.
   With KnapsackSOS.block0_psd..block4_psd this makes the d = 4 Gram bridge
   fully kernel-checked; the residual Python-pinned layer is only the
   z-vector Gram derivation (harmonic completeness, validated by 130x130
   spectral reconstruction) and the standard moment/SOS duality.

   Every emitted identity was validated in exact Fraction arithmetic against
   gram_block_fast AND g_scalar * v * v before emission (anti-phantom gate);
   the Lean kernel is the final gate. -/
import Mathlib
import KnapsackSOS

set_option maxHeartbeats 1600000
set_option linter.unusedTactic false

namespace KnapsackSOS
namespace BridgeD4

theorem entry_k0_i0_j0 (n : ℚ) (hn : (7 : ℚ) < n) :
    (1) * f n 0
      = g0 n * v n 0 0 * v n 0 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i1_j0 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n) * f n 1
      = g0 n * v n 0 1 * v n 0 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i1_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^2 - n) * f n 2 + (n) * f n 1
      = g0 n * v n 0 1 * v n 0 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i2_j0 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^2/2 - n/2) * f n 2
      = g0 n * v n 0 2 * v n 0 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i2_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^3/2 - 3*n^2/2 + n) * f n 3 + (n^2 - n) * f n 2
      = g0 n * v n 0 2 * v n 0 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i2_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^4/4 - 3*n^3/2 + 11*n^2/4 - 3*n/2) * f n 4 + (n^3 - 3*n^2 + 2*n) * f n 3 + (n^2/2 - n/2) * f n 2
      = g0 n * v n 0 2 * v n 0 2 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i3_j0 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^3/6 - n^2/2 + n/3) * f n 3
      = g0 n * v n 0 3 * v n 0 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i3_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^4/6 - n^3 + 11*n^2/6 - n) * f n 4 + (n^3/2 - 3*n^2/2 + n) * f n 3
      = g0 n * v n 0 3 * v n 0 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i3_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^5/12 - 5*n^4/6 + 35*n^3/12 - 25*n^2/6 + 2*n) * f n 5 + (n^4/2 - 3*n^3 + 11*n^2/2 - 3*n) * f n 4 + (n^3/2 - 3*n^2/2 + n) * f n 3
      = g0 n * v n 0 3 * v n 0 2 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i3_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^6/36 - 5*n^5/12 + 85*n^4/36 - 25*n^3/4 + 137*n^2/18 - 10*n/3) * f n 6 + (n^5/4 - 5*n^4/2 + 35*n^3/4 - 25*n^2/2 + 6*n) * f n 5 + (n^4/2 - 3*n^3 + 11*n^2/2 - 3*n) * f n 4 + (n^3/6 - n^2/2 + n/3) * f n 3
      = g0 n * v n 0 3 * v n 0 3 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i4_j0 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^4/24 - n^3/4 + 11*n^2/24 - n/4) * f n 4
      = g0 n * v n 0 4 * v n 0 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i4_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^5/24 - 5*n^4/12 + 35*n^3/24 - 25*n^2/12 + n) * f n 5 + (n^4/6 - n^3 + 11*n^2/6 - n) * f n 4
      = g0 n * v n 0 4 * v n 0 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i4_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^6/48 - 5*n^5/16 + 85*n^4/48 - 75*n^3/16 + 137*n^2/24 - 5*n/2) * f n 6 + (n^5/6 - 5*n^4/3 + 35*n^3/6 - 25*n^2/3 + 4*n) * f n 5 + (n^4/4 - 3*n^3/2 + 11*n^2/4 - 3*n/2) * f n 4
      = g0 n * v n 0 4 * v n 0 2 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i4_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^7/144 - 7*n^6/48 + 175*n^5/144 - 245*n^4/48 + 203*n^3/18 - 49*n^2/4 + 5*n) * f n 7 + (n^6/12 - 5*n^5/4 + 85*n^4/12 - 75*n^3/4 + 137*n^2/6 - 10*n) * f n 6 + (n^5/4 - 5*n^4/2 + 35*n^3/4 - 25*n^2/2 + 6*n) * f n 5 + (n^4/6 - n^3 + 11*n^2/6 - n) * f n 4
      = g0 n * v n 0 4 * v n 0 3 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k0_i4_j4 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^8/576 - 7*n^7/144 + 161*n^6/288 - 245*n^5/72 + 6769*n^4/576 - 3283*n^3/144 + 363*n^2/16 - 35*n/4) * f n 8 + (n^7/36 - 7*n^6/12 + 175*n^5/36 - 245*n^4/12 + 406*n^3/9 - 49*n^2 + 20*n) * f n 7 + (n^6/8 - 15*n^5/8 + 85*n^4/8 - 225*n^3/8 + 137*n^2/4 - 15*n) * f n 6 + (n^5/6 - 5*n^4/3 + 35*n^3/6 - 25*n^2/3 + 4*n) * f n 5 + (n^4/24 - n^3/4 + 11*n^2/24 - n/4) * f n 4
      = g0 n * v n 0 4 * v n 0 4 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g0, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i1_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-2) * f n 2 + (2) * f n 1
      = g1 n * v n 1 0 * v n 1 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i2_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (4 - 2*n) * f n 3 + (2*n - 4) * f n 2
      = g1 n * v n 1 1 * v n 1 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i2_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-2*n^2 + 10*n - 12) * f n 4 + (2*n^2 - 12*n + 16) * f n 3 + (2*n - 4) * f n 2
      = g1 n * v n 1 1 * v n 1 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i3_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-n^2 + 5*n - 6) * f n 4 + (n^2 - 5*n + 6) * f n 3
      = g1 n * v n 1 2 * v n 1 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i3_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-n^3 + 9*n^2 - 26*n + 24) * f n 5 + (n^3 - 11*n^2 + 36*n - 36) * f n 4 + (2*n^2 - 10*n + 12) * f n 3
      = g1 n * v n 1 2 * v n 1 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i3_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-n^4/2 + 7*n^3 - 71*n^2/2 + 77*n - 60) * f n 6 + (n^4/2 - 9*n^3 + 107*n^2/2 - 129*n + 108) * f n 5 + (2*n^3 - 19*n^2 + 57*n - 54) * f n 4 + (n^2 - 5*n + 6) * f n 3
      = g1 n * v n 1 2 * v n 1 2 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i4_j1 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-n^3/3 + 3*n^2 - 26*n/3 + 8) * f n 5 + (n^3/3 - 3*n^2 + 26*n/3 - 8) * f n 4
      = g1 n * v n 1 3 * v n 1 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i4_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-n^4/3 + 14*n^3/3 - 71*n^2/3 + 154*n/3 - 40) * f n 6 + (n^4/3 - 17*n^3/3 + 98*n^2/3 - 232*n/3 + 64) * f n 5 + (n^3 - 9*n^2 + 26*n - 24) * f n 4
      = g1 n * v n 1 3 * v n 1 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i4_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-n^5/6 + 10*n^4/3 - 155*n^3/6 + 290*n^2/3 - 174*n + 120) * f n 7 + (n^5/6 - 13*n^4/3 + 239*n^3/6 - 503*n^2/3 + 328*n - 240) * f n 6 + (n^4 - 15*n^3 + 80*n^2 - 180*n + 144) * f n 5 + (n^3 - 9*n^2 + 26*n - 24) * f n 4
      = g1 n * v n 1 3 * v n 1 2 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k1_i4_j4 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-n^6/18 + 3*n^5/2 - 295*n^4/18 + 185*n^3/2 - 2552*n^2/9 + 446*n - 280) * f n 8 + (n^6/18 - 2*n^5 + 475*n^4/18 - 170*n^3 + 5162*n^2/9 - 968*n + 640) * f n 7 + (n^5/2 - 11*n^4 + 183*n^3/2 - 361*n^2 + 676*n - 480) * f n 6 + (n^4 - 43*n^3/3 + 74*n^2 - 488*n/3 + 128) * f n 5 + (n^3/3 - 3*n^2 + 26*n/3 - 8) * f n 4
      = g1 n * v n 1 3 * v n 1 3 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g1, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k2_i2_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (4) * f n 4 + (-8) * f n 3 + (4) * f n 2
      = g2 n * v n 2 0 * v n 2 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g2, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k2_i3_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (4*n - 16) * f n 5 + (32 - 8*n) * f n 4 + (4*n - 16) * f n 3
      = g2 n * v n 2 1 * v n 2 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g2, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k2_i3_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (4*n^2 - 36*n + 80) * f n 6 + (-8*n^2 + 76*n - 176) * f n 5 + (4*n^2 - 44*n + 112) * f n 4 + (4*n - 16) * f n 3
      = g2 n * v n 2 1 * v n 2 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g2, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k2_i4_j2 (n : ℚ) (hn : (7 : ℚ) < n) :
    (2*n^2 - 18*n + 40) * f n 6 + (-4*n^2 + 36*n - 80) * f n 5 + (2*n^2 - 18*n + 40) * f n 4
      = g2 n * v n 2 2 * v n 2 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g2, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k2_i4_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (2*n^3 - 30*n^2 + 148*n - 240) * f n 7 + (-4*n^3 + 64*n^2 - 332*n + 560) * f n 6 + (2*n^3 - 38*n^2 + 220*n - 400) * f n 5 + (4*n^2 - 36*n + 80) * f n 4
      = g2 n * v n 2 2 * v n 2 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g2, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k2_i4_j4 (n : ℚ) (hn : (7 : ℚ) < n) :
    (n^4 - 22*n^3 + 179*n^2 - 638*n + 840) * f n 8 + (-2*n^4 + 48*n^3 - 418*n^2 + 1572*n - 2160) * f n 7 + (n^4 - 30*n^3 + 301*n^2 - 1248*n + 1840) * f n 6 + (4*n^3 - 64*n^2 + 332*n - 560) * f n 5 + (2*n^2 - 18*n + 40) * f n 4
      = g2 n * v n 2 2 * v n 2 2 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g2, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k3_i3_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-8) * f n 6 + (24) * f n 5 + (-24) * f n 4 + (8) * f n 3
      = g3 n * v n 3 0 * v n 3 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g3, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k3_i4_j3 (n : ℚ) (hn : (7 : ℚ) < n) :
    (48 - 8*n) * f n 7 + (24*n - 144) * f n 6 + (144 - 24*n) * f n 5 + (8*n - 48) * f n 4
      = g3 n * v n 3 1 * v n 3 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g3, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k3_i4_j4 (n : ℚ) (hn : (7 : ℚ) < n) :
    (-8*n^2 + 104*n - 336) * f n 8 + (24*n^2 - 320*n + 1056) * f n 7 + (-24*n^2 + 336*n - 1152) * f n 6 + (8*n^2 - 128*n + 480) * f n 5 + (8*n - 48) * f n 4
      = g3 n * v n 3 1 * v n 3 1 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g3, f, v]
  push_cast
  field_simp
  all_goals ring

theorem entry_k4_i4_j4 (n : ℚ) (hn : (7 : ℚ) < n) :
    (16) * f n 8 + (-64) * f n 7 + (96) * f n 6 + (-64) * f n 5 + (16) * f n 4
      = g4 n * v n 4 0 * v n 4 0 := by
  have h0 : n ≠ 0 := ne_of_gt (by linarith)
  have h1 : n - 1 ≠ 0 := ne_of_gt (by linarith)
  have h2 : n - 2 ≠ 0 := ne_of_gt (by linarith)
  have h3 : n - 3 ≠ 0 := ne_of_gt (by linarith)
  have h4 : n - 4 ≠ 0 := ne_of_gt (by linarith)
  have h5 : n - 5 ≠ 0 := ne_of_gt (by linarith)
  have h6 : n - 6 ≠ 0 := ne_of_gt (by linarith)
  have h7 : n - 7 ≠ 0 := ne_of_gt (by linarith)
  simp only [g4, f, v]
  push_cast
  field_simp
  all_goals ring

end BridgeD4
end KnapsackSOS

#print axioms KnapsackSOS.BridgeD4.entry_k0_i0_j0
#print axioms KnapsackSOS.BridgeD4.entry_k0_i1_j0
#print axioms KnapsackSOS.BridgeD4.entry_k0_i1_j1
#print axioms KnapsackSOS.BridgeD4.entry_k0_i2_j0
#print axioms KnapsackSOS.BridgeD4.entry_k0_i2_j1
#print axioms KnapsackSOS.BridgeD4.entry_k0_i2_j2
#print axioms KnapsackSOS.BridgeD4.entry_k0_i3_j0
#print axioms KnapsackSOS.BridgeD4.entry_k0_i3_j1
#print axioms KnapsackSOS.BridgeD4.entry_k0_i3_j2
#print axioms KnapsackSOS.BridgeD4.entry_k0_i3_j3
#print axioms KnapsackSOS.BridgeD4.entry_k0_i4_j0
#print axioms KnapsackSOS.BridgeD4.entry_k0_i4_j1
#print axioms KnapsackSOS.BridgeD4.entry_k0_i4_j2
#print axioms KnapsackSOS.BridgeD4.entry_k0_i4_j3
#print axioms KnapsackSOS.BridgeD4.entry_k0_i4_j4
#print axioms KnapsackSOS.BridgeD4.entry_k1_i1_j1
#print axioms KnapsackSOS.BridgeD4.entry_k1_i2_j1
#print axioms KnapsackSOS.BridgeD4.entry_k1_i2_j2
#print axioms KnapsackSOS.BridgeD4.entry_k1_i3_j1
#print axioms KnapsackSOS.BridgeD4.entry_k1_i3_j2
#print axioms KnapsackSOS.BridgeD4.entry_k1_i3_j3
#print axioms KnapsackSOS.BridgeD4.entry_k1_i4_j1
#print axioms KnapsackSOS.BridgeD4.entry_k1_i4_j2
#print axioms KnapsackSOS.BridgeD4.entry_k1_i4_j3
#print axioms KnapsackSOS.BridgeD4.entry_k1_i4_j4
#print axioms KnapsackSOS.BridgeD4.entry_k2_i2_j2
#print axioms KnapsackSOS.BridgeD4.entry_k2_i3_j2
#print axioms KnapsackSOS.BridgeD4.entry_k2_i3_j3
#print axioms KnapsackSOS.BridgeD4.entry_k2_i4_j2
#print axioms KnapsackSOS.BridgeD4.entry_k2_i4_j3
#print axioms KnapsackSOS.BridgeD4.entry_k2_i4_j4
#print axioms KnapsackSOS.BridgeD4.entry_k3_i3_j3
#print axioms KnapsackSOS.BridgeD4.entry_k3_i4_j3
#print axioms KnapsackSOS.BridgeD4.entry_k3_i4_j4
#print axioms KnapsackSOS.BridgeD4.entry_k4_i4_j4
