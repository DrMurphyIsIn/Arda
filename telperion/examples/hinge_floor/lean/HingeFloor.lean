/- Generated by telperion.hinge — the BG G1 L2 profile→equal-children
   reduction: Σ(yᵢ−t0)₊ ≥ (Σyᵢ − k·t0)₊ (posPart subadditivity), = the
   Jensen 'min at equal children' step for the convex hinge φ=c·(y−t0)₊.
   DO NOT EDIT BY HAND. -/

import Mathlib

namespace HingeFloor

set_option maxHeartbeats 400000 in
theorem hinge_floor_k2 (c t0 y0 y1 : ℝ) (hc : 0 ≤ c) :
    c * ((y0 + y1) - 2 * t0)⁺ ≤ c * ((y0 - t0)⁺ + (y1 - t0)⁺) := by
  have hsum : (y0 + y1) - 2 * t0 = (y0 - t0) + (y1 - t0) := by ring
  have hsub : ((y0 + y1) - 2 * t0)⁺ ≤ (y0 - t0)⁺ + (y1 - t0)⁺ := by
    rw [hsum, posPart_def]
    refine sup_le ?_ (by positivity)
    gcongr <;> exact le_posPart _
  exact mul_le_mul_of_nonneg_left hsub hc

set_option maxHeartbeats 400000 in
theorem hinge_floor_k3 (c t0 y0 y1 y2 : ℝ) (hc : 0 ≤ c) :
    c * ((y0 + y1 + y2) - 3 * t0)⁺ ≤ c * ((y0 - t0)⁺ + (y1 - t0)⁺ + (y2 - t0)⁺) := by
  have hsum : (y0 + y1 + y2) - 3 * t0 = (y0 - t0) + (y1 - t0) + (y2 - t0) := by ring
  have hsub : ((y0 + y1 + y2) - 3 * t0)⁺ ≤ (y0 - t0)⁺ + (y1 - t0)⁺ + (y2 - t0)⁺ := by
    rw [hsum, posPart_def]
    refine sup_le ?_ (by positivity)
    gcongr <;> exact le_posPart _
  exact mul_le_mul_of_nonneg_left hsub hc

set_option maxHeartbeats 400000 in
theorem hinge_floor_k4 (c t0 y0 y1 y2 y3 : ℝ) (hc : 0 ≤ c) :
    c * ((y0 + y1 + y2 + y3) - 4 * t0)⁺ ≤ c * ((y0 - t0)⁺ + (y1 - t0)⁺ + (y2 - t0)⁺ + (y3 - t0)⁺) := by
  have hsum : (y0 + y1 + y2 + y3) - 4 * t0 = (y0 - t0) + (y1 - t0) + (y2 - t0) + (y3 - t0) := by ring
  have hsub : ((y0 + y1 + y2 + y3) - 4 * t0)⁺ ≤ (y0 - t0)⁺ + (y1 - t0)⁺ + (y2 - t0)⁺ + (y3 - t0)⁺ := by
    rw [hsum, posPart_def]
    refine sup_le ?_ (by positivity)
    gcongr <;> exact le_posPart _
  exact mul_le_mul_of_nonneg_left hsub hc

set_option maxHeartbeats 400000 in
theorem hinge_floor_k5 (c t0 y0 y1 y2 y3 y4 : ℝ) (hc : 0 ≤ c) :
    c * ((y0 + y1 + y2 + y3 + y4) - 5 * t0)⁺ ≤ c * ((y0 - t0)⁺ + (y1 - t0)⁺ + (y2 - t0)⁺ + (y3 - t0)⁺ + (y4 - t0)⁺) := by
  have hsum : (y0 + y1 + y2 + y3 + y4) - 5 * t0 = (y0 - t0) + (y1 - t0) + (y2 - t0) + (y3 - t0) + (y4 - t0) := by ring
  have hsub : ((y0 + y1 + y2 + y3 + y4) - 5 * t0)⁺ ≤ (y0 - t0)⁺ + (y1 - t0)⁺ + (y2 - t0)⁺ + (y3 - t0)⁺ + (y4 - t0)⁺ := by
    rw [hsum, posPart_def]
    refine sup_le ?_ (by positivity)
    gcongr <;> exact le_posPart _
  exact mul_le_mul_of_nonneg_left hsub hc

end HingeFloor
