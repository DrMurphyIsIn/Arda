/-  H11K_Slab_U11004.lean -- lane H11K: the zero-free edge-clearance slab above the top edge T = 11004
    (GENERATED by scratchpad h11k/emit_slab.py via Arb4_emit_seg.emit_slabs_file, UNTRUSTED; re-checked here).  Each slab: the certificate data
    `Arb4.SlabD` (cells planned by emit_h1000_offline.plan_slab, unchanged) and ONE
    `decide +kernel` of `Arb4.slabOK` (budget, Pochhammer bound, every cell with its evaluator
    run and rectangle geometry), then `Arb4.slabOK_sound`.
    Slabs: U11004.
    conjecture1_proved = False.  Finite verification up to a fixed height only.
-/
import Arb4_Slab

open Arb4 Arb4.Q

namespace H11K.Slab

/-- The slab `(0, 1) x [11004, 11004003/1000]` (3 cells, N = 3302). -/
noncomputable def S_U11004 : SlabD where
  tq := 14
  N := 3302
  L := (Q.frac 8103 1000)
  R := (Q.frac 417 5000)
  lo := (Q.ofNat 11004)
  hi := (Q.add (Q.ofNat 11004) (Q.frac 3 1000))
  U := (Q.frac 11005 1)
  Qr := 34690000000000000000000000000000000000000000000000000
  r0 := 57
  FN := 13
  FD := 25
  m := 3
  xs := fun j => [(Q.frac 1 2), (Q.frac 2 3), (Q.frac 5 6), (Q.frac 1 1)].getD j (Q.frac 1 1)
  cell := fun j => [{ a := 7, b := 0, q := 12, tn := 180289561, rn := 834, rd := 10000, w := (Q.frac 1 12), hh := (Q.frac 25 16384) },
    { a := 3, b := 0, q := 4, tn := 180289561, rn := 834, rd := 10000, w := (Q.frac 1 12), hh := (Q.frac 25 16384) },
    { a := 11, b := 0, q := 12, tn := 180289561, rn := 834, rd := 10000, w := (Q.frac 1 12), hh := (Q.frac 25 16384) }].getD j { a := 11, b := 0, q := 12, tn := 180289561, rn := 834, rd := 10000, w := (Q.frac 1 12), hh := (Q.frac 25 16384) }

theorem S_U11004_ok : slabOK S_U11004 = true := by decide +kernel

/-- **The zero-free slab `SlabClear (11004 : ℝ) ((11004 : ℝ) + 3 / 1000)`, hypothesis-free.** -/
theorem S_U11004_clear : EdgeClearGlue.SlabClear (11004 : ℝ) ((11004 : ℝ) + 3 / 1000) := by
  have h := slabOK_sound S_U11004 S_U11004_ok
  have hlo : S_U11004.lo.val = (11004 : ℝ) := (Q.cv_nat 11004)
  have hhi : S_U11004.hi.val = ((11004 : ℝ) + 3 / 1000) := (Q.cv_add _ _ _ _ (Q.cv_nat 11004) (Q.cv_frac 3 1000))
  rw [hlo, hhi] at h
  exact h

end H11K.Slab
