/-
  DO NOT EDIT BY HAND -- generated by examples/speiser_box_probe/generate.py.

  The certificate ARITHMETIC for the MM_speiser_box_probe Face-7 pilot box: the exact
  rational inequalities that make the Lipschitz net exclude a zero of `deriv riemannZeta`
  on [1/4, 3/8] x [6, 10].  Corrupting the certificate (understating `delta`, inflating
  `boundL`, shrinking `boundM`, loosening the grid) makes these `norm_num` goals FAIL.

  NOT the Speiser wall: Speiser (1935) makes non-vanishing of `zeta'` on the WHOLE open
  left strip equivalent to RH, and no finite union of boxes exhausts a strip.  Verifying
  this box decides nothing about RH.  conjecture1_proved = False.
-/
/- telperion 0.1.6 | family SpeiserBoxCert | input-hash 8eb59fecce110ebe
   5 theorems, 5 generation-time self-checks passed.
   Regenerate & verify:  forge diff --family <module:attr> --manifest <manifest.json> --check
   DO NOT EDIT BY HAND — edits are flagged by the regeneration diff.  -/

import Mathlib

namespace SpeiserBoxCert

/-- Certificate constants for `speiser_box_cert` (grid-modulus non-vanishing).

    Box `[1/4, 3/8] x [6, 10]`; a single grid column at
    `re = 5/16` with 8 rows `25/4, 27/4, 29/4, 31/4, 33/4, 35/4, 37/4, 39/4`; cell half-extents
    `(1/16, 1/4)`.

    Constants: `M = 3/10` (claimed `sup |f''|` on the box),
    `delta = 13/50` (covering radius), `L = 1/10` (claimed `min |f'|` on the grid).

    Re-derived numerically at precision 30 and again at 60:
    observed `sup |f''| = 0.2871904959`, observed `min |f'| = 0.2019822134`.
    TRUST CLASS mpmath-numeric -- EVIDENCE, NOT A PROOF.  The Lean obligations stay open. -/
def speiser_box_cert_boundM : ℚ := 3/10

def speiser_box_cert_delta : ℚ := 13/50

def speiser_box_cert_boundL : ℚ := 1/10

def speiser_box_cert_halfWidth : ℚ := 1/16

def speiser_box_cert_halfHeight : ℚ := 1/4

/-- The claimed covering radius dominates the exact cell half-diagonal. -/
theorem speiser_box_cert_cover_radius_ok : speiser_box_cert_halfWidth ^ 2 + speiser_box_cert_halfHeight ^ 2 ≤ speiser_box_cert_delta ^ 2 := by
  unfold speiser_box_cert_halfWidth speiser_box_cert_halfHeight speiser_box_cert_delta; norm_num

/-- **The gap condition** `M * delta < L`: the single inequality that makes the net
    exclude a zero.  Everything else in the certificate feeds it. -/
theorem speiser_box_cert_gap_ok : speiser_box_cert_boundM * speiser_box_cert_delta < speiser_box_cert_boundL := by
  unfold speiser_box_cert_boundM speiser_box_cert_delta speiser_box_cert_boundL; norm_num

/-- The grid column spans the box in the real direction within `halfWidth`. -/
theorem speiser_box_cert_column_span_ok :
    ((5/16 : ℚ) - 1/4 ≤ speiser_box_cert_halfWidth ∧
     (3/8 : ℚ) - 5/16 ≤ speiser_box_cert_halfWidth) := by
  unfold speiser_box_cert_halfWidth; constructor <;> norm_num

/-- The grid rows tile the imaginary range within `halfHeight`. -/
theorem speiser_box_cert_row_tiling_ok :
    ((25/4 : ℚ) - speiser_box_cert_halfHeight ≤ 6 ∧
     (39/4 : ℚ) + speiser_box_cert_halfHeight ≥ 10) := by
  unfold speiser_box_cert_halfHeight; constructor <;> norm_num

/-- **The certificate**, as one proposition: covering radius, gap condition, column span
    and row tiling together.  These are exactly the facts the Python checker re-derived;
    a forged certificate makes one of them FALSE and the kernel rejects this theorem. -/
theorem speiser_box_cert :
    (speiser_box_cert_halfWidth ^ 2 + speiser_box_cert_halfHeight ^ 2 ≤ speiser_box_cert_delta ^ 2) ∧ (speiser_box_cert_boundM * speiser_box_cert_delta < speiser_box_cert_boundL) ∧
    ((5/16 : ℚ) - 1/4 ≤ speiser_box_cert_halfWidth ∧
     (3/8 : ℚ) - 5/16 ≤ speiser_box_cert_halfWidth) ∧
    ((25/4 : ℚ) - speiser_box_cert_halfHeight ≤ 6 ∧
     (39/4 : ℚ) + speiser_box_cert_halfHeight ≥ 10) :=
  ⟨speiser_box_cert_cover_radius_ok, speiser_box_cert_gap_ok, speiser_box_cert_column_span_ok, speiser_box_cert_row_tiling_ok⟩

end SpeiserBoxCert
