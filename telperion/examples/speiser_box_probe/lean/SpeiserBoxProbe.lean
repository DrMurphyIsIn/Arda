/-
  MM_speiser_box_probe island roll-up.

  NOT the Speiser wall, and not a step toward RH: a single finitely-checkable rectangle
  for `deriv riemannZeta`, in the T5 winding-certificate face retargeted at `zeta'`.
  `conjecture1_proved = False`.
-/
import SpeiserBoxProbe.GridNonvanishing
import SpeiserBoxProbe.SpeiserBox
import SpeiserBoxProbe.Cert
