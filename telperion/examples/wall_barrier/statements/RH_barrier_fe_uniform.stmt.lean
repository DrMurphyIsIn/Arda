-- Requires OP 1 of RH_BARRIER_FE_UNIFORMITY_DESIGN_2026-09-18.md: the FEBarrier AUTHORED
-- block (OnLine, FEData, RHfor) in Statements.RHDefs, regenerated by build_rhdefs.py.
theorem RH_barrier_fe_uniform :
    (∀ (Crit : FEBarrier.FEData → Prop),
        (∀ E : FEBarrier.FEData, Crit E ↔ FEBarrier.RHfor E) →
        ∀ (D : FEBarrier.FEData) (ρ : ℂ), D.Ξ ρ = 0 → ¬ FEBarrier.OnLine ρ →
          ¬ (∀ E : FEBarrier.FEData, Crit E))
      ∧ (∀ (L : FEBarrier.FEData → Prop),
          (∀ E : FEBarrier.FEData, L E → FEBarrier.RHfor E) →
          ∀ (D : FEBarrier.FEData) (ρ : ℂ), D.Ξ ρ = 0 → ¬ FEBarrier.OnLine ρ →
            ¬ (∀ E : FEBarrier.FEData, L E)) := by
  sorry
