created = "2026-09-23"
depends_on = ["AND_stirling_binet_k1"]
kind = "lemma"
name = "AND.theta_branch"
statement_module = "Statements.AND_theta_branch"
status = "proved"
title = "Riemann-Siegel brick B0 (theta): for every t > 0 the Gauss-product branch limit of Im log Gamma(1/4 + it/2) exists and lies within 1/t of the RS main term after the (t/2) log pi shift. A height-uniform phase bound for the finite-verification ladder, NOT RH. conjecture1_proved = False"
updated = "2026-09-24"

[comparator]
artifact_sha256 = "35c4dcb74e8b56cc7362ac49a664707a786ccce38598355f5dd798fa760cb3c3"
date = "2026-09-24"
run_id = "36054938938"
run_url = "https://github.com/DrMurphyIsIn/Arda/actions/runs/36054938938"
theorem = "RSDesignTheta.gaussBranch_theta_sub_thetaMain_le"

[proof]
artifact = "../../examples/zeta_reflection/lean/RSTheta.lean"
artifact_kind = "lean_module"
closure_clean = true
via = "direct"

[readback]
auditor = "blind-auditors A1+A2 (independent, 2026-09-23)"
date = "2026-09-23"
independence = "unverified"
text = "Read-back, written before opening the artifact: for every real t > 0 there is a real Lambda such that the Gauss-product partial values imLnVal(1/4, t/2, n) = (t/2) log n - sum_{k=0}^{n} arctan((t/2)/(1/4+k)) converge to Lambda. That Lambda is the continuous-branch Im log Gamma(1/4 + it/2). The claim is |Lambda - (t/2) log pi - thetaMain t| <= 1/t, where thetaMain t = (t/2) log t - (t/2) log(2 pi) - t/2 - pi/8. This is a height-uniform bound on the Riemann-Siegel theta phase, not RH.\n\nGate: the canonical path (R.load_campaign, then V.statement_matches against V._normalized_statement) returns True. A whitespace-normalized comparison also matches exactly. artifact_incompleteness_markers returns [] on RSTheta, ThetaConverge, ThetaValue, ArctanTaylor and DlvpTheta.\n\nBuild: `lake build RSTheta` succeeded (8661 jobs) with no 'declaration uses' warnings.\n\nOwn probe: `#print axioms RSDesignTheta.gaussBranch_theta_sub_thetaMain_le` gives [propext, Classical.choice, Quot.sound]. The registry statement text, re-elaborated outside the namespace as an example, type-checked against the theorem. The probe has been deleted.\n\nHidden hypotheses: the only hypothesis is ht : 0 < t. convergence_obligation needs 0 < x and Gamma != 0, and the proof discharges both. No hmem/hArb/hLine binders reach the theorem; the only hmem is a local `have`. The tokens axiom, opaque and native_decide appear only in docstrings.\n\nVocabulary: the ANDDefs mirrors of ThetaGap.imLnVal and ZeroFreeBridge.thetaMain are character-identical to the island definitions (ThetaValue.lean:180-181, DlvpTheta.lean:243-244). The artifact imports those real definitions through zzl_core -> zero_free_bridge and does not shadow them.\n\nmpmath (thetaMain reimplemented from the Lean definition; theta matches siegeltheta): the gap theta - thetaMain is 0.0441 at t=1, 2.08e-3 at t=10, 2.08e-4 at t=100 and 2.08e-6 at t=10000. It behaves like 1/(48t), well within 1/t. Partial sums converge to Im loggamma.\n\nEstablishes: kernel-checked convergence of the series plus the phase bound for all t > 0, with no `sorry`. Does not establish: any zero location or verification height, or RH. conjecture1_proved = False.\n\nIndependent second read-back (A2): Read-back, written before I opened the artifact: for every real t > 0 there is a Λ such that the Gauss-product partial values ThetaGap.imLnVal (1/4) (t/2) n = (t/2) log n - sum_{k<=n} arctan((t/2)/(1/4+k)) converge to Λ. That limit is the continuous branch of Im log Gamma(1/4 + it/2). Also |Λ - (t/2) log π - thetaMain t| <= 1/t, where thetaMain t = (t/2) log t - (t/2) log(2π) - t/2 - π/8. The only hypothesis is t > 0.\n\nResults:\n- Statement match: the statement text is identical to RSTheta.lean:512-514 modulo whitespace. On the canonical path, V.statement_matches(RSTheta text, V._normalized_statement(node, root)) returned True.\n- Incompleteness markers: V.artifact_incompleteness_markers returned [] on every local module in the closure (RSTheta, ThetaConverge, ThetaValue, ArctanTaylor, DlvpTheta). The rest of the closure is Mathlib.\n- Build: leanlock lake build RSTheta exited 0 (8661 jobs). There were only linter warnings and no \"declaration uses\" warning.\n- Axioms: my probe's #print axioms returned [propext, Classical.choice, Quot.sound]. #check matches the registry proposition exactly. I deleted the probe.\n- Hidden hypotheses: none. The only binder is ht. In code there is no axiom, opaque, unsafe, extern, implemented_by, native_decide, ofReduceBool, hArb or hLine. The only hmem hits are a local interval-membership `have` in trapezoid_convex, which is not an oracle.\n- Vocabulary: the ANDDefs mirrors of ThetaGap.imLnVal (ThetaValue.lean:180-181) and ZeroFreeBridge.thetaMain (DlvpTheta.lean:243-244) are character-identical to the island definitions, in the same namespaces. `rfl` against the mirror bodies also typechecked for both.\n- Numerics (mpmath, dps 40): θ - thetaMain = 0.0441, 2.08e-3, 2.08e-4 and 2.08e-6 at t = 1, 10, 100, 10000. Each is <= 1/t; the difference is about 1/(48t). A Richardson extrapolation of imLnVal at t = 1 and 10 matches Im loggamma, so the Gauss branch is the principal one.\n\nWhat this establishes: a height-uniform, kernel-checked RS theta phase bound, which is one brick of the finite-verification ladder. It is not RH and says nothing about zeros or conjecture1. conjecture1_proved = False.\n\nTestimony: docs/AUDIT_TESTIMONY_AND_theta_branch_2026-09-23_A1.md, _A2.md (both pass, axioms_clean, statement_byte_identical)."
