-- DO NOT EDIT BY HAND — generated by telperion mission; node AND_checkline_correct
-- VERBATIM statement of checkLine_correct (CheckBand.lean:234-244, v4.32
-- zeta_reflection island, namespace ZetaReflection; consumer defs DIntvProd/gLine
-- mirrored in ANDDefs).
import Mathlib
import Statements.ANDDefs
open ZetaReflection XiLineZeros

theorem checkLine_correct
    (d : BandData) (T0 T1 : ℝ) (n : ℕ)
    (hlen : d.boxes.length = n + 1)
    (p : Fin (n + 1) → ℝ) (hmono : StrictMono p)
    (hlo : T0 ≤ p 0) (hhi : p (Fin.last n) ≤ T1)
    (hmem : ∀ i : Fin (n + 1), DIntvProd.DIntv.memR (gLine (p i))
      (d.boxes.get (Fin.cast hlen.symm i)))
    (hchk : d.check = true) :
    ∃ xs : List ℝ, xs.length = n ∧ xs.IsChain (· < ·) ∧
      (∀ t ∈ xs, T0 ≤ t ∧ t ≤ T1) ∧
      (∀ t ∈ xs, completedRiemannZeta (1 / 2 + (t : ℂ) * Complex.I) = 0) := by sorry
