-- DO NOT EDIT BY HAND — generated by telperion mission; node RH_weil_window_floor_of_certified_block; sha256 65215e793763e7f3
-- AUTHORED statement (NOT a verbatim island extract): the ZHU COMPACT-WINDOW REDUCTION --
-- Xuefeng Zhu, arXiv:2608.24827 v2 (2026-09-02), "Weil positivity in compact windows: a finite
-- reduction, certified two-sided bounds, and a Landau-Widom decay law", THEOREM 1.1, with the
-- parity extension of Lemma 6.1 / Corollary 6.3.
--
-- RE-SPECIFIED 2026-09-23 (DRAFT node; provenance hash changes).  Until 2026-09-23 the four
-- analytic inputs of Theorem 1.1 were carried as OPAQUE predicates.  They are now CONCRETE
-- definitions in RHDefs (namespace WeilWindow), mirrored verbatim from the rvm_bridge island:
--   SymbolRepresentation L  (eq. 2)   -- PROVED for every L: RvMBridgeZhu.symbolRepresentation
--                                        (ZhuSymbol.lean); therefore NO LONGER a hypothesis here.
--   EnvelopeBound           (Lemma 3.1) -- PROVED: RvMBridgeZhu.envelopeBound (ZhuEnvelope.lean);
--                                        NO LONGER a hypothesis here.
--   LegendreLocalization L Tsharp N epsD epsB  (eqs. 6, 12, 13) -- the tail data of the Legendre
--                                        block decomposition: Gershgorin deviation of the tail
--                                        block <= epsD, Schur sums of the coupling <= epsB, on the
--                                        CONCRETE reduced matrix beta* I + 2 p p^T + C.  The analytic
--                                        content behind it, eq. (6) (Legendre-mode transform =
--                                        spherical Bessel) and eq. (12) (|j_n(x)| <= x^n/(2n+1)!!),
--                                        is PROVED (ZhuLegendre.lean); the tail SUMS are the
--                                        paper's numerically evaluated constants (both < 1e-100 in
--                                        the certified run) and remain a hypothesis: hloc.
--   ReducedHeadFloor L Tsharp lam0 N -- UNCHANGED, OPAQUE: the Arb/mpmath certified least-eigenvalue
--                                        floor lam0 of the leading N x N block.  The trust seam.
-- Two hypotheses were ADDED, both honest side conditions the opaque draft lacked:
--   hT   : 0 < Tsharp      -- the split point is a positive height (eq. 4 integrates over [0, T#]).
--   hodd : OddSectorFloor  -- Theorem 1.1 and the certified even-mode block cover REAL EVEN f;
--                             WindowFloor quantifies over complex f, which needs Zhu Lemma 6.1
--                             (Q(f) = Q(Re f_e) + Q(Im f_e) + Q(Re f_o) + Q(Im f_o)) and the ODD
--                             sector's own certified floor (eq. 14, 8.2e-15 at L = 0.8).  Carried
--                             explicitly so an even-mode certificate never speaks for complex f.
-- The conclusion WindowFloor L (min lam0 (beta - epsD) - epsB) is UNCHANGED.
--
-- WHAT REMAINS FOR A PROOF (not attempted; recorded in telperion/docs/ZHU_INPUTS_DISCHARGE_2026-09-23.md):
-- the frequency split (eq. 4, from EnvelopeBound + SymbolRepresentation), the Legendre-basis
-- representation of the reduced form R (Legendre completeness in L^2[-L, L]), the two-block bound
-- (eq. 13), the parity decoupling (Lemma 6.1), and -- structurally -- the fact that hhead is opaque:
-- no proof term can use it, so the node stays DRAFT by design until a registry trust-seam policy exists.
--
-- THE FINITE PART IS REAL AND WAS RE-DERIVED.  hA and hbeta pin the two constants to their
-- definitions rather than to printed values: A_L = sum_(log n < 2L) 2*Lambda(n)/sqrt(n) is a finite
-- von Mangoldt sum (three terms at L = 0.8, namely n = 2, 3, 4) and beta* = log(T#/2pi) - 1/T# - A_L.
-- Both were recomputed independently at 40 digits and reproduce the paper exactly: A_L = 2.9419735,
-- beta* = 0.5134667 at T# = 200.  The paper's OWN retracted support-2.38 claim substituted the
-- per-prime constant A_eff = 4.6948 for A_L = 7.0750 at L = 1.19; the telperion emitter
-- `window_form_floor` refuses exactly that substitution.
--
-- NUMERICAL INSTANTIATION (NOT asserted by this node).  Zhu Theorem 1.2 runs the reduction at
-- L = 0.8, T# = 200, N = 200 even Legendre modes, 50 digits: lam0 = 9e-18, epsD, epsB <= 1e-100,
-- giving Q(f) >= 8.9e-18 ||f||_2^2 for supp f in [-0.8, 0.8].  This node asserts NO number.
--
-- SCOPE, BLUNTLY.  WindowFloor L lam with lam > 0 at a FIXED L is a finite fragment of RH and is
-- what RH predicts; it confirms nothing.  The RH-equivalent clause is WindowFloor L 0 for EVERY L
-- (Weil 1952; Bombieri 2000), and nothing here reduces, weakens or approaches it.  Zhu's own
-- Theorem 1.4 proves this route closed: T# > T_1 = 2*pi*e^(A_L) is forced, A_L = (4 + o(1)) e^L by
-- PNT, so the matrix size grows doubly exponentially in the support.
--
-- Design memos: telperion/docs/ZHU_WINDOW_POSITIVITY_IMPORT_2026-09-19.md (import),
-- telperion/docs/DESIGN_RH_weil_window_floor_of_certified_block_2026-09-22.md (why it was opaque),
-- telperion/docs/ZHU_INPUTS_DISCHARGE_2026-09-23.md (the re-specification and what was proved).
-- Emitter: telperion/src/telperion/emit_window_form_floor.py (kind `window_form_floor`).
-- Depends on the proved node RH_limit_explicit_formula.  No RH progress.  PRE-WALL.
-- conjecture1_proved = False.
import Mathlib
import Statements.RHDefs

theorem weil_window_floor_of_certified_block
    (L Tsharp A beta lam0 epsD epsB : ℝ) (N : ℕ)
    -- the FINITE data, pinned to its definitions (re-derived, never trusted)
    (hA : A = WeilWindow.combMass L)
    (hbeta : beta = WeilWindow.betaStar L Tsharp)
    (hT : 0 < Tsharp)
    (hbpos : 0 < beta)
    (hcut : Real.exp 1 * L * Tsharp / 2 ≤ 2 * (N : ℝ))
    -- the Legendre tail data of Zhu eq. (13), CONCRETE (its analytic inputs eqs. 6 and 12 are proved)
    (hloc : WeilWindow.LegendreLocalization L Tsharp N epsD epsB)
    -- the NUMERICAL certification: the Arb / mpmath trust seam, OPAQUE by design
    (hhead : WeilWindow.ReducedHeadFloor L Tsharp lam0 N)
    -- the ODD parity sector (Zhu Lemma 6.1, eq. 14), CONCRETE, carried explicitly
    (hodd : WeilWindow.OddSectorFloor L (min lam0 (beta - epsD) - epsB))
    (hepsD : 0 ≤ epsD) (hepsB : 0 ≤ epsB)
    (hpos : 0 < min lam0 (beta - epsD) - epsB) :
    WeilWindow.WindowFloor L (min lam0 (beta - epsD) - epsB) := by sorry
