-- DO NOT EDIT BY HAND — generated by telperion mission; node RH_weil_window_floor_of_certified_block; sha256 8d0bdb0b971ec222
-- AUTHORED statement (NOT a verbatim island extract): the ZHU COMPACT-WINDOW REDUCTION --
-- Xuefeng Zhu, arXiv:2608.24827 v2 (2026-09-02), "Weil positivity in compact windows: a finite
-- reduction, certified two-sided bounds, and a Landau-Widom decay law", THEOREM 1.1.
--
-- WHAT IS CLAIMED, AND WHAT IS ASSUMED.  This node is CONDITIONAL and stated only.  Zhu's
-- "one-stroke reduction" of the infinite-dimensional window problem to a single finite PSD matrix
-- is one step on paper and FOUR undischarged analytic steps in Lean.  All four are carried here as
-- named, opaque hypotheses, together with the numerical certification, which is a non-kernel trust
-- boundary of exactly the kind the zero-localization ladder already documents:
--   hQrep : WeilWindow.SymbolRepresentation L           -- eq. (2), the frequency-side rearrangement
--   henv  : WeilWindow.EnvelopeBound                    -- Lemma 3.1, the digamma envelope
--   hloc  : WeilWindow.LegendreLocalization ...         -- eqs. (6) and (12), spherical-Bessel decay
--   hhead : WeilWindow.ReducedHeadFloor ...             -- the Arb/mpmath certified block floor lam0
-- The two moves of Theorem 1.1 that ARE elementary and provable in Lean -- the two-block bound
-- (eq. 13, lambda_min(M_R) >= min(lambda_min A, lambda_min D) - ||B||) and the Cholesky residual
-- bound (Lemma 5.2) -- are the discharge targets; neither is proved here.
--
-- THE FINITE PART IS REAL AND WAS RE-DERIVED.  hA and hbeta pin the two constants to their
-- definitions rather than to printed values: A_L = sum_(log n < 2L) 2*Lambda(n)/sqrt(n) is a finite
-- von Mangoldt sum (three terms at L = 0.8, namely n = 2, 3, 4) and beta* = log(T#/2pi) - 1/T# - A_L.
-- Both were recomputed independently at 40 digits and reproduce the paper exactly: A_L = 2.9419735,
-- beta* = 0.5134667 at T# = 200, and the whole T_1 threshold table of Remark 3.3.  That re-derivation
-- is load-bearing: the paper's OWN retracted support-2.38 claim substituted the per-prime constant
-- A_eff = 4.6948 for A_L = 7.0750 at L = 1.19, which bounds the comb from BELOW where the envelope
-- needs an UPPER bound, and every downstream number stayed self-consistent.  The telperion emitter
-- `window_form_floor` refuses exactly that substitution.
--
-- NUMERICAL INSTANTIATION (NOT asserted by this node).  Zhu Theorem 1.2 runs the reduction at
-- L = 0.8, T# = 200, N = 200 even Legendre modes, 50 digits: lam0 = 9e-18, epsD, epsB <= 1e-100,
-- giving Q(f) >= 8.9e-18 ||f||_2^2 for supp f in [-0.8, 0.8], i.e. unconditional Weil positivity on
-- autocorrelation support 1.6 -- 2.3 times the classical (log 2)/2 range (Yoshida; Connes-Consani).
-- With Corollary 6.3 the parity and reality restrictions come off, and Theorem 6.2 certifies a
-- simple even ground state at that support.  This node asserts NO number; the numbers enter through
-- hhead, and the emitted Lean instances prove only that the published rounding is sound.
--
-- SCOPE, BLUNTLY.  WindowFloor L lam with lam > 0 at a FIXED L is a finite fragment of RH and is
-- what RH predicts; it confirms nothing.  The RH-equivalent clause is WindowFloor L 0 for EVERY L
-- (Weil 1952; Bombieri 2000), and nothing here reduces, weakens or approaches it.  Zhu's own
-- Theorem 1.4 proves this route closed: T# > T_1 = 2*pi*e^(A_L) is forced, A_L = (4 + o(1)) e^L by
-- PNT, so the matrix size grows doubly exponentially in the support and certification past support
-- ~3.2 (T_1 ~ 1e7) is computationally void; by Lemma 3.2 the threshold cannot be lowered within
-- pointwise-envelope certificates.  The accompanying decay law -ln lambda*(L) ~ 2 pi^2 N(T*)/ln N(T*)
-- with T* = 2 pi e^(2L) is MEASURED and its constant FITTED (Remarks 1.5 and 12.2), never proved,
-- and must not be cited as a theorem.  Theorem 1.4 is scoped to certificates that bound the prime
-- comb POINTWISE; it is not a barrier for positivity arguments in general.
--
-- Design memo: telperion/docs/ZHU_WINDOW_POSITIVITY_IMPORT_2026-09-19.md.
-- Emitter: telperion/src/telperion/emit_window_form_floor.py (kind `window_form_floor`).
-- Depends on the proved node RH_limit_explicit_formula, which supplies weilForm's identification
-- with the sum over the zeros and is what makes a window statement expressible at all.  The
-- WeilForm names used by WeilWindow.WindowFloor are a VERBATIM mirror of the weil_form_enclosure
-- island's WeilFormDefs.lean, so island and registry cannot silently decouple.
-- No RH progress.  PRE-WALL.  conjecture1_proved = False.
import Mathlib
import Statements.RHDefs

theorem weil_window_floor_of_certified_block
    (L Tsharp A beta lam0 epsD epsB : ℝ) (N : ℕ)
    -- the FINITE data, pinned to its definitions (re-derived, never trusted)
    (hA : A = WeilWindow.combMass L)
    (hbeta : beta = WeilWindow.betaStar L Tsharp)
    (hbpos : 0 < beta)
    (hcut : Real.exp 1 * L * Tsharp / 2 ≤ 2 * (N : ℝ))
    -- the ANALYTIC chain of Zhu Thm 1.1, NAMED AND UNDISCHARGED
    (hQrep : WeilWindow.SymbolRepresentation L)
    (henv : WeilWindow.EnvelopeBound)
    (hloc : WeilWindow.LegendreLocalization L Tsharp N epsD epsB)
    -- the NUMERICAL certification: the Arb / mpmath trust seam
    (hhead : WeilWindow.ReducedHeadFloor L Tsharp lam0 N)
    (hepsD : 0 ≤ epsD) (hepsB : 0 ≤ epsB)
    (hpos : 0 < min lam0 (beta - epsD) - epsB) :
    WeilWindow.WindowFloor L (min lam0 (beta - epsD) - epsB) := by sorry
