-- DO NOT EDIT BY HAND — generated by telperion mission; node RH_limit_explicit_formula; sha256 0de1d1ff639c9997
-- AUTHORED statement (NOT a verbatim island extract): the LIMIT EXPLICIT FORMULA -- routes-roadmap
-- E8 = B6 = D6 (with E7), the unconditional Guinand-Weil identity at all heights in diffraction
-- form.  The T -> infinity limit of the finite, kernel-verified DiffractionCore.rect_explicit_formula
-- (whose g == 1 specialisation is the Bragg bridge rect_explicit_formula_bragg): for every smooth
-- compactly supported g : R -> C (WeilExplicit.IsWeilTest, the Weil 1952 class; Iwaniec-Kowalski
-- Thm 5.12 normalisation), with H_g(s) = int g(u) e^{(s-1/2)u} du (= h(gamma) at s = 1/2 + i gamma),
--   Sum_rho m(rho) H_g(rho)  =  h(i/2) + h(-i/2) - g(0) log pi + (1/2pi) int h(r) Re psi(1/4 + ir/2) dr
--                              - Sum_n Lambda(n)/sqrt(n) (g(log n) + g(-log n)),
-- the zero side summed over ALL nontrivial zeros with the registry multiplicity weight
-- (WeilExplicit.zeroMult = the RvMCount.zetaZeroCount divisor), stated as HasSum so summability is
-- part of the claim (a tsum of a non-summable family would be 0); the archimedean integrand is
-- asserted Integrable for the same reason.  The prime side is a finite sum (compact support), so
-- the PNT growth of Sum Lambda(n)/sqrt n (roadmap section 1) does not enter; the transform is entire,
-- so off-line zeros are seen literally (roadmap D3 Paley-Wiener).  No evenness hypothesis.
-- Consumes RH_corridor_bound (horizontal edges -> 0) and RH_rvm_unconditional (absolute convergence
-- of the zero side).  Design memo: telperion/docs/E8_LIMIT_EXPLICIT_FORMULA_DESIGN_2026-09-18.md.
-- Flagged for the blind read-back audit cycle.  conjecture1_proved = False.
import Mathlib
import Statements.RHDefs
open MeasureTheory

theorem limit_explicit_formula (g : ℝ → ℂ) (hg : WeilExplicit.IsWeilTest g) :
    Integrable (WeilExplicit.archIntegrand g) ∧
    HasSum (fun ρ : ℂ => (WeilExplicit.zeroMult ρ : ℂ) * WeilExplicit.weilKernel g ρ)
      (WeilExplicit.archSide g - WeilExplicit.primeSide g) := by sorry
