created = "2026-09-21"
depends_on = ["RH_xi_right_deriv_bound", "RH_xi_diff_regular_of_growth"]
kind = "lemma"
name = "RH.xi_growth_of_two"
statement_module = "Statements.RH_xi_growth_of_two"
status = "proved"
title = "XI PARTIAL FRACTION, obligation 1 REDUCED to two classical inequalities (2026-09-21): the log-growth bound on Re s >= 1/2 follows from LocalCountSum (Sum m(rho)/(1+(Im rho - a)^2) = O(log a), the textbook consequence of the local zero count) and StripDerivBound (Landau's local partial fraction transferred to the derivative on 1/4 <= Re s <= 9/4 by Cauchy + Stirling). Plan correction recorded: on Re s >= 2 the double-pole sum is O(log t), not O(1). Both in flight (E6Bridge23/24). conjecture1_proved = False."
updated = "2026-09-21"

[proof]
artifact = "../../examples/rvm_bridge/lean/E6Bridge22.lean"
artifact_kind = "lean_module"
closure_clean = true
via = "direct"

[readback]
auditor = "blind-auditor-xi-growth-2026-09-21 (AUDIT_TESTIMONY_XI_GROWTH_2026-09-21.md)"
date = "2026-09-21"
text = "Read-back: the entire extension's logarithmic growth on Re s >= 1/2 follows from LocalCountSum and StripDerivBound (compact rectangle by continuity; strip region inside the closed StripDerivBound region; right half-plane by the right derivative bound plus the local-count sum; continuity across the isolated zeros). Plan correction confirmed: on Re s >= 2 the double-pole sum is O(log t). A reduction. conjecture1_proved = False."
