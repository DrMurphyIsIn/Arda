/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_euler_factor_section_offline.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_euler_factor_section_offline.lean (header sha256 fa3c783e7bc1b9d7), binders and conclusion verbatim; the
   PROOF is the artifact constant `EulerFactorSectionOffline.euler_factor_section_offline` from EulerFactorSectionOffline. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import EulerFactorSectionOffline

namespace EulerFactorSectionOffline

open Quasicrystal

theorem _root_.MissionJudge.MM_euler_factor_section_offline :
    ¬ (∀ x : ℂ,
        twoFreq 1 ((-(1 / Real.sqrt 2) : ℝ) : ℂ) 0 (-(Real.log 2)) x = 0 → x.im = 0) :=
  EulerFactorSectionOffline.euler_factor_section_offline

end EulerFactorSectionOffline
