/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_selfinversive_iff_hardyz_real.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_selfinversive_iff_hardyz_real.lean (header sha256 8cfa120c735a6388), binders and conclusion verbatim; the
   PROOF is the artifact constant `Quasicrystal.selfInversive_iff_hardyZ_real` from InvolutionDictionary. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import InvolutionDictionary

namespace Quasicrystal

open Polynomial Complex

theorem _root_.MissionJudge.MM_selfinversive_iff_hardyz_real :
    ∀ {p : ℂ[X]} {phase u : ℂ}
    (hid : ∀ z : ℂ, z ≠ 0 →
      z ^ p.natDegree * (starRingEnd ℂ) (p.eval (1 / (starRingEnd ℂ) z))
        = phase * p.eval z)
    (hu_norm : ‖u‖ = 1) (hu_sq : u ^ 2 = phase) (ω : ℝ) (x : ℝ), (hardyZ p u ω x).im = 0 :=
  Quasicrystal.selfInversive_iff_hardyZ_real

end Quasicrystal
