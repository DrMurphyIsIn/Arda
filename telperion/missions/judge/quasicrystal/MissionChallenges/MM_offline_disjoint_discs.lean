/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_offline_disjoint_discs.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_offline_disjoint_discs.lean (header sha256 bac57ccef7c3f828), binders and conclusion verbatim; the
   PROOF is the artifact constant `Quasicrystal.offline_disjoint_discs` from OfflineDiscs. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import OfflineDiscs

namespace Quasicrystal

theorem _root_.MissionJudge.MM_offline_disjoint_discs :
    ∀ (S : Finset ℂ)
    (hstrip : ∀ z ∈ S, 0 < z.re ∧ z.re < 1), ∃ r : ℝ, 0 < r ∧
      (∀ z ∈ S, ∀ w ∈ S, z ≠ w → Disjoint (Metric.closedBall z r) (Metric.closedBall w r)) ∧
      (∀ z ∈ S, Metric.closedBall z r ⊆ {s : ℂ | 0 < s.re ∧ s.re < 1}) :=
  Quasicrystal.offline_disjoint_discs

end Quasicrystal
