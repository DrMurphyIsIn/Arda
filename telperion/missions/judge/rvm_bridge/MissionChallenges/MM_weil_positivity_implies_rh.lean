/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_weil_positivity_implies_rh.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_weil_positivity_implies_rh.lean (header sha256 495d1d8aa1d2491d), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge9.weil_positivity_implies_rh` from E6Bridge9. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge9
import AxiomGuardRvMBridge

namespace RvMBridge9

open WeilExplicit

theorem _root_.MissionJudge.MM_weil_positivity_implies_rh :
    ∀ (hpos : ∀ g : ℝ → ℂ, WeilExplicit.IsWeilTest g → 0 ≤ (WeilExplicit.weilForm (WeilExplicit.autocorr g)).re), RiemannHypothesis :=
  RvMBridge9.weil_positivity_implies_rh

end RvMBridge9
