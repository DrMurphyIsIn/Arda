/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_xi_right_deriv_bound.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_xi_right_deriv_bound.lean (header sha256 a241f18193bcbfa2), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge22.rightDerivBound` from E6Bridge22. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge22
import AxiomGuardRvMBridge

namespace RvMBridge22

open Zeta23 Complex MeasureTheory Filter Topology Metric
open scoped ComplexConjugate
open WeilExplicit RvMBridge18 RvMBridge20 RvMBridgeXi
open scoped LSeries.notation ArithmeticFunction

theorem _root_.MissionJudge.RH_xi_right_deriv_bound :
    RightDerivBound :=
  RvMBridge22.rightDerivBound

end RvMBridge22
