/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_limit_explicit_formula.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_limit_explicit_formula.lean (header sha256 0de1d1ff639c9997), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge4.limit_explicit_formula` from E6Bridge4. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge4
import AxiomGuardRvMBridge

namespace RvMBridge4

open Zeta23 Complex MeasureTheory
open MeasureTheory Complex
open WeilExplicit

theorem _root_.MissionJudge.RH_limit_explicit_formula :
    ∀ (g : ℝ → ℂ) (hg : WeilExplicit.IsWeilTest g), Integrable (WeilExplicit.archIntegrand g) ∧
    HasSum (fun ρ : ℂ => (WeilExplicit.zeroMult ρ : ℂ) * WeilExplicit.weilKernel g ρ)
      (WeilExplicit.archSide g - WeilExplicit.primeSide g) :=
  RvMBridge4.limit_explicit_formula

end RvMBridge4
