/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_weil_positivity_implies_rh_of_gaussian.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_weil_positivity_implies_rh_of_gaussian.lean (header sha256 63922d12bed8def7), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge6.weil_positivity_implies_rh_of` from E6Bridge6. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge6
import AxiomGuardRvMBridge

namespace RvMBridge6

open Zeta23 Complex MeasureTheory Filter Topology
open scoped ComplexConjugate
open WeilExplicit

theorem _root_.MissionJudge.MM_weil_positivity_implies_rh_of_gaussian :
    ∀ (hO1 : GaussianTransfer) (hO2 : GaussianDominance)
    (hpos : ∀ g : ℝ → ℂ, WeilExplicit.IsWeilTest g →
      0 ≤ (WeilExplicit.weilForm (WeilExplicit.autocorr g)).re), RiemannHypothesis :=
  RvMBridge6.weil_positivity_implies_rh_of

end RvMBridge6
