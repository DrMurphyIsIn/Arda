/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_gaussian_positivity_of_window.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_gaussian_positivity_of_window.lean (header sha256 e11dd7173e40c90e), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge12.gaussian_positivity_of_window` from E6Bridge12. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge12
import AxiomGuardRvMBridge

namespace RvMBridge12

open Zeta23 Complex MeasureTheory Filter Topology
open scoped ComplexConjugate
open WeilExplicit RvMBridge6 RvMBridge7 RvMBridgeGauss

theorem _root_.MissionJudge.MM_gaussian_positivity_of_window :
    ∀ {c D d δ lam : ℝ} (hD : 0 ≤ D) (hδ : 0 < δ)
    (hgap : d ^ 2 + 1 / 4 < D ^ 2) (hwin : WindowOnLine c D)
    (hnear : ∃ ρ : ℂ, IsNontrivialZero ρ ∧ δ ≤ |ρ.im - c| ∧ |ρ.im - c| ≤ d)
    (hlam : lamThreshold c D d δ ≤ lam), 0 ≤ (zeroSide (gaussTest c lam)).re :=
  RvMBridge12.gaussian_positivity_of_window

end RvMBridge12
