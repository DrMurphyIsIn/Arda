/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_rh_implies_weil_positivity.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_rh_implies_weil_positivity.lean (header sha256 bbc7d281bc379a43), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge5.rh_implies_weil_positivity` from E6Bridge5. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge5
import AxiomGuardRvMBridge

namespace RvMBridge5

open Zeta23 Complex MeasureTheory
open scoped ComplexConjugate Convolution
open MeasureTheory Complex
open WeilExplicit

theorem _root_.MissionJudge.MM_rh_implies_weil_positivity :
    ∀ (hRH : RiemannHypothesis), ∀ g : ℝ → ℂ, WeilExplicit.IsWeilTest g → 0 ≤ (WeilExplicit.weilForm (WeilExplicit.autocorr g)).re :=
  RvMBridge5.rh_implies_weil_positivity

end RvMBridge5
