/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_bl_explicit_formula_of_strip.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_bl_explicit_formula_of_strip.lean (header sha256 7c8c7c0332b81507), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge26.bl_explicit_formula_of_strip` from E6Bridge26. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge26
import AxiomGuardRvMBridge

namespace RvMBridge26

open Filter Topology

theorem _root_.MissionJudge.RH_bl_explicit_formula_of_strip :
    ∀ (h2 : RvMBridge22.StripDerivBound) (n : ℕ) (hn : 0 < n), Tendsto (RvMBridge15.BombieriLagarias.liZeroSum n) atTop
      (𝓝 (RvMBridge15.BombieriLagarias.archSide n + RvMBridge15.BombieriLagarias.finiteSide n)) :=
  RvMBridge26.bl_explicit_formula_of_strip

end RvMBridge26
