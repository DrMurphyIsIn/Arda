/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node mirrormere/MM_gaussian_explicit_formula.
   The TYPE below is the registered statement, missions/mirrormere/lean/Statements/
   MM_gaussian_explicit_formula.lean (header sha256 420e6cc4ccecfef9), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMBridge10.zeroSide_gaussTest_eq` from E6Bridge10. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import E6Bridge10
import AxiomGuardRvMBridge

namespace RvMBridge10

open Zeta23 Complex MeasureTheory Filter Topology
open scoped ComplexConjugate
open WeilExplicit RvMBridge6 RvMBridge8 RvMBridgeGauss

theorem _root_.MissionJudge.MM_gaussian_explicit_formula :
    ∀ (c lam : ℝ) (hlam : 0 < lam), RvMBridge6.zeroSide (RvMBridge6.gaussTest c lam)
      = WeilExplicit.archSide (WeilExplicit.autocorr (RvMBridge8.gaussPhi c lam))
        - WeilExplicit.primeSide (WeilExplicit.autocorr (RvMBridge8.gaussPhi c lam)) :=
  RvMBridge10.zeroSide_gaussTest_eq

end RvMBridge10
