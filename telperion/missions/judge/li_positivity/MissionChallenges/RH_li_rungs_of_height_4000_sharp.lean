/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_li_rungs_of_height_4000_sharp.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_li_rungs_of_height_4000_sharp.lean (header sha256 f07141b4e612cf3e), binders and conclusion verbatim; the
   PROOF is the artifact constant `LiLadderHeight.li_rungs_of_bands_4000_upto_sharp` from LiLadderSharp. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import LiLadderSharp
import AxiomGuardLiPositivity

namespace LiLadderHeight

open scoped Real
open LiCriterion
open LiFacePrelude

theorem _root_.MissionJudge.RH_li_rungs_of_height_4000_sharp :
    ∀ (hall : ∀ ρ : ℂ, riemannZeta ρ = 0 → 0 < ρ.im → ρ.im ≤ 4000 → ρ.re = 1 / 2), ∀ n : ℕ, n ≤ 25128 → 0 ≤ (taylorCoeff riemannXi n).re :=
  LiLadderHeight.li_rungs_of_bands_4000_upto_sharp

end LiLadderHeight
