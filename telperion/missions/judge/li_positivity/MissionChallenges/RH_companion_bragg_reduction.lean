/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_companion_bragg_reduction.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_companion_bragg_reduction.lean (header sha256 179e8131187f63bb), binders and conclusion verbatim; the
   PROOF is the artifact constant `RvMWeierstrass.taylorCoeff_companion_bragg_of_exhaustion_limits` from RvMCompanionBraggLimit. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import RvMCompanionBraggLimit
import AxiomGuardLiPositivity

namespace RvMWeierstrass

open Complex Filter Topology

theorem _root_.MissionJudge.RH_companion_bragg_reduction :
    ∀ (n : ℕ)
    (zeroSum boundary bragg : ℕ → ℂ) (boundaryLim braggLim : ℂ)
    (hformula : ∀ k, 2 * (Real.pi : ℂ) * I * zeroSum k = boundary k - bragg k)
    (hzero : Tendsto (fun k => 2 * (Real.pi : ℂ) * I * zeroSum k) atTop
        (𝓝 (2 * (Real.pi : ℂ) * I * LiCriterion.taylorCoeff LiCriterion.riemannXi n)))
    (hboundary : Tendsto boundary atTop (𝓝 boundaryLim))
    (hbragg : Tendsto bragg atTop (𝓝 braggLim)), LiCriterion.taylorCoeff DiffractionCore.zetaPoleCompanion n
      = (boundaryLim - braggLim) / (2 * (Real.pi : ℂ) * I)
        - 1 - LiCriterion.taylorCoeff Complex.Gammaℝ n :=
  RvMWeierstrass.taylorCoeff_companion_bragg_of_exhaustion_limits

end RvMWeierstrass
