/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_dlvp_region_effective.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_dlvp_region_effective.lean (header sha256 ee5a8607789a7959), binders and conclusion verbatim; the
   PROOF is the artifact constant `ZeroFreeBridge.dlvp_zeta_region_rate_effective` from DlvpZetaRateEffective. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import DlvpZetaRateEffective
import AxiomGuardLiPositivity

namespace ZeroFreeBridge

open Complex MeromorphicOn Metric

theorem _root_.MissionJudge.RH_dlvp_region_effective :
    ∀ (β γ : ℝ) (k : ℤ)
    (h34 : 3/4 ≤ β) (hβ1 : β < 1) (hΓ : 55/16 ≤ |γ|) (hk : 1 ≤ k)
    (hmρ₀ : divisor riemannZeta (ball ((2 : ℂ) + (γ : ℂ) * I) (11/8)) ((β : ℂ) + (γ : ℂ) * I) = k), β ≤ 1 - dlvpRateC / Real.log |γ| :=
  ZeroFreeBridge.dlvp_zeta_region_rate_effective

end ZeroFreeBridge
