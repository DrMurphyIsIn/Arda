/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_li_rungs_of_height_4000.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_li_rungs_of_height_4000.lean (header sha256 f4977db040200582), binders and conclusion verbatim; the
   PROOF is the artifact constant `LiLadderHeight.li_rungs_of_bands_4000_upto` from LiLadderHeight. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import LiLadderHeight
import AxiomGuardLiPositivity

namespace LiLadderHeight

open scoped Real
open LiCriterion
open LiFacePrelude

theorem _root_.MissionJudge.RH_li_rungs_of_height_4000 :
    ∀ (hall : ∀ ρ : ℂ, riemannZeta ρ = 0 → 0 < ρ.im → ρ.im ≤ 4000 → ρ.re = 1 / 2), ∀ n : ℕ, n ≤ 18848 → 0 ≤ (taylorCoeff riemannXi n).re :=
  LiLadderHeight.li_rungs_of_bands_4000_upto

end LiLadderHeight
