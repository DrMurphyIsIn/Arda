/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_backlund_s_log.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_backlund_s_log.lean (header sha256 ec00ae361e114283), binders and conclusion verbatim; the
   PROOF is the artifact constant `Backlund.riemannS_abs_le_log_of_ne_zero` from RvMBacklundLogCont. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import RvMBacklundLogCont
import AxiomGuardLiPositivity

namespace Backlund

open Complex MeasureTheory

theorem _root_.MissionJudge.RH_backlund_s_log :
    ∀ {T : ℝ} (hT : 4 ≤ T)
    (hζne : ∀ x ∈ Set.Icc (1 / 2 : ℝ) 2, riemannZeta ((x : ℂ) + (T : ℂ) * I) ≠ 0), |DiffractionCore.riemannS T|
      ≤ Real.log ((4 * T + 19) / ‖backlundAux T ((2 : ℝ) : ℂ)‖) / Real.log (7 / 4 / (3 / 2)) + 2 :=
  Backlund.riemannS_abs_le_log_of_ne_zero

end Backlund
