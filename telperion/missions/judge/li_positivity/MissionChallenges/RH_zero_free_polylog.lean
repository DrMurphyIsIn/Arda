/- DO NOT EDIT BY HAND -- generated by telperion.missions.judge.
   Comparator CHALLENGE for registry node rh/RH_zero_free_polylog.
   The TYPE below is the registered statement, missions/rh/lean/Statements/
   RH_zero_free_polylog.lean (header sha256 02299e57afc7c2b7), binders and conclusion verbatim; the
   PROOF is the artifact constant `ZeroFreeBridge.riemannZeta_zero_free_polylog` from ZeroFreePolylog. Both kernels
   accept this module only if the artifact proves exactly the registered proposition.
   The AxiomGuard imports load the whole island, so a vocabulary constant shadowed by
   the artifact is a duplicate declaration here, not a silent substitution. The
   `namespace` and the `open` lines inside it are the artifact's own at its
   declaration, so every name in the statement resolves exactly as it does there. -/
import ZeroFreePolylog
import AxiomGuardZeroFree

namespace ZeroFreeBridge

open Filter Topology

theorem _root_.MissionJudge.RH_zero_free_polylog :
    ∃ c > (0 : ℝ), ∀ β γ : ℝ,
      riemannZeta ((β : ℂ) + γ * Complex.I) = 0 → 2 ≤ γ →
      β ≤ 1 - c / (γ ^ 4 * (1 + Real.log (2 * γ))) :=
  ZeroFreeBridge.riemannZeta_zero_free_polylog

end ZeroFreeBridge
