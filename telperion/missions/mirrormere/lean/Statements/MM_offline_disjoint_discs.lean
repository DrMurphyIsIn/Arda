-- DO NOT EDIT BY HAND — generated by telperion mission; node MM_offline_disjoint_discs; sha256 bac57ccef7c3f828
-- AUTHORED statement (NOT a verbatim island extract): the QC_RECURRENCE section 4.3
-- isolation lemma — finitely many distinct points of the open critical strip admit
-- pairwise-disjoint closed discs of a common positive radius contained in the strip.
-- Pure Mathlib metric topology.  Flagged for the blind read-back audit cycle.
import Mathlib

theorem offline_disjoint_discs (S : Finset ℂ)
    (hstrip : ∀ z ∈ S, 0 < z.re ∧ z.re < 1) :
    ∃ r : ℝ, 0 < r ∧
      (∀ z ∈ S, ∀ w ∈ S, z ≠ w → Disjoint (Metric.closedBall z r) (Metric.closedBall w r)) ∧
      (∀ z ∈ S, Metric.closedBall z r ⊆ {s : ℂ | 0 < s.re ∧ s.re < 1}) := by sorry
