-- DO NOT EDIT BY HAND — generated by telperion mission; node MM_torus_section_n2_rigidity; sha256 ae6668c61cff9908
-- AUTHORED statement (NOT a verbatim island extract): the N=2 rigidity rung stated in
-- the ladder vocabulary (AUTHORED defs in MMDefs); mathematically equivalent to the
-- island's twoFreq_realRooted_iff via the dictionary identity — the discharge is a
-- short bridge, recorded in the node.  Flagged for the blind read-back audit cycle.
import Mathlib
import Statements.MMDefs
open Quasicrystal

theorem torus_section_n2_rigidity (c₁ c₂ : ℂ) (lam₁ lam₂ : ℝ)
    (hc₁ : c₁ ≠ 0) (hc₂ : c₂ ≠ 0) (hlam : lam₁ ≠ lam₂) :
    (∀ x : ℂ, linearTorusForm 2 ![c₁, c₂] (torusOrbit 2 ![lam₁, lam₂] x) = 0 → x.im = 0)
      ↔ ‖c₁‖ = ‖c₂‖ := by sorry
