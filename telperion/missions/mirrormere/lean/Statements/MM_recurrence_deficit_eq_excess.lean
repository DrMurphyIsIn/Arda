-- DO NOT EDIT BY HAND — generated by telperion mission; node MM_recurrence_deficit_eq_excess
-- AUTHORED statement (NOT a verbatim island extract): the W3d recurrence-dictionary
-- bridge lemma per QC_RECURRENCE section 4.2.  recurrenceDeficit is AUTHORED in
-- MMDefs (flagged); excess = Aoff - Aon = e^(1/10) + e^(-1/10) - 2 is the verbatim
-- BraggDefect vocabulary.  Content: the algebraic identity (e^d - 1)(1 - e^(-d)) =
-- e^d + e^(-d) - 2 at d = 1/10, plus strict positivity for all positive d.
-- Flagged for the blind read-back audit cycle.
import Mathlib
import Statements.MMDefs
open Quasicrystal BraggDefect

theorem recurrence_deficit_eq_excess :
    recurrenceDeficit (1 / 10) = excess ∧
    ∀ δ : ℝ, 0 < δ → 0 < recurrenceDeficit δ := by sorry
