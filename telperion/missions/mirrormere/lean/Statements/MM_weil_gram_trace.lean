-- DO NOT EDIT BY HAND — generated by telperion mission; node MM_weil_gram_trace; sha256 a98e9a276f1dfd4c
-- AUTHORED statement (NOT a verbatim island extract): routes-roadmap D2, the primes-side
-- Weil-Gram matrix of a finite test family.  Design memo
-- telperion/docs/MM_mm-d2-weil-gram-trace_DESIGN_2026-09-18.md.
--
-- Vocabulary: the WeilExplicit block of MMDefs -- six definitions MIRRORED VERBATIM from
-- the RH registry's E8 block (RHDefs.lean, branch rh/e8-statement) plus the two AUTHORED
-- D2 definitions crossCorr and weilGram.
--
-- READ IT AS: D1 (MM_rect_trace_reading) read the FINITE Bragg bridge as a trace identity
-- at one box.  D2 packages the INFINITE-height trace identity (E8) as a finite Hermitian
-- MATRIX over a test family.  Conjunct 1 is Hermitian-ness, which holds on the PRIMES side
-- alone (crossCorr g2 g1 u = conj (crossCorr g1 g2 (-u)); Re psi is real) -- NO zero
-- symmetry and no unproved input.  Conjunct 2 is the trace reading: each entry is the sum
-- over ALL rho : C, weighted by the registry divisor multiplicity zeroMult, of the
-- factorised kernel weilKernel (g i) rho * conj (weilKernel (g j) (1 - conj rho)); on the
-- critical line 1 - conj rho = rho, so the diagonal summand is |h_i(gamma)|^2 and an
-- off-line zero is seen at a COMPLEX ordinate (no analytic continuation: weilKernel is one
-- Bochner integral, entire).  Conjunct 3 is the quadratic-form reading that ties the matrix
-- to RHLinalg.hermForm, hence to the defect instrument (MM_offline_pairs_le_defect).
--
-- NOT CLAIMED: nothing about the inertia of weilGram for a general family.  `posIndex
-- (weilGram g) = k` (equivalently `defect = 0`) for ALL g IS Weil's criterion and therefore
-- RH; for ONE g it is data for the instruments.  Neither is a conjunct here.
-- conjecture1_proved = False.
--
-- HONEST INTEGRALS, NOT HYPOTHESISED: crossCorr (g i) (g j) is itself smooth with compact
-- support when g i and g j are (support inside supp (g i) - supp (g j); smoothness from
-- Mathlib's HasCompactSupport.contDiff_convolution_*), so the Bochner integrals inside
-- archSide/primeSide applied to it are honest and not junk-valued.  That is a THEOREM on the
-- proof island (lemma name crossCorr_isWeilTest), deliberately NOT carried as a hypothesis
-- here: a hypothesis would let a vacuous instantiation satisfy the node.
--
-- Third conjunct, conjugation convention (memo section 6, TRAP 4): hermForm A x is
-- Re (sum_{i,j} conj (x i) * A i j * x j), while crossCorr is conjugate-linear in its
-- SECOND argument, so the test function whose autocorrelation carries the form is built
-- from the CONJUGATED coefficient vector.  Writing `sum i, x i * g i v` there instead makes
-- the conjunct FALSE (a 2x2 counterexample is in memo section 6).  crossCorr G G is the
-- Weil autocorrelation `autocorr G`; it is spelled through crossCorr so that this node does
-- not depend on which branch lands the `autocorr` alias first.
--
-- Flagged for the blind read-back audit cycle.
import Mathlib
import Statements.MMDefs
open Complex MeasureTheory
open WeilExplicit

theorem weil_gram_trace (k : ℕ) (g : Fin k → ℝ → ℂ) (hg : ∀ i, IsWeilTest (g i)) :
    (weilGram g).IsHermitian ∧
    (∀ i j : Fin k,
      HasSum (fun ρ : ℂ => (zeroMult ρ : ℂ) * weilKernel (g i) ρ
          * (starRingEnd ℂ) (weilKernel (g j) (1 - (starRingEnd ℂ) ρ)))
        (weilGram g i j)) ∧
    (∀ (x : Fin k → ℂ) (G : ℝ → ℂ),
      G = (fun v => ∑ i, (starRingEnd ℂ) (x i) * g i v) →
      RHLinalg.hermForm (weilGram g) x
        = (archSide (crossCorr G G) - primeSide (crossCorr G G)).re) := by sorry
