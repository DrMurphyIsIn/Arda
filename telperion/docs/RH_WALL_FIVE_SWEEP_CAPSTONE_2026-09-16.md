# The RH wall — a five-sweep no-go map (campaign capstone)

*Five honest adversarial multi-agent sweeps (`wylptmzxv` weight, `wryhnwohw` ordinate,
`w0fy2g125` horizontal, `wu0aecd5x` joint, `wcrkqbzrj` non-self-dual GL(n); ~60 agents,
literature web-verified, corpus read against disk). Result: the RH companion wall is
**irreducible from every direction the campaign could identify.** `conjecture1_proved =
False.` A verified no-go map — it explains why RH resists every current class of approach;
it does not breach it.*

## The five directions, all closed
| Direction | Sweep | Verdict |
|---|---|---|
| Weight (multiplicative) | `wylptmzxv` | FREE — does no work |
| Ordinate (zero statistics) | `wryhnwohw` | ORTHOGONAL — reflection-blind |
| Horizontal (real-part) | `w0fy2g125` | FREE-OR-RH — one static landmark, no lever |
| Joint (real-part × ordinate) | `wu0aecd5x` | FORCED symmetry theorem; escape = non-robust arithmetic |
| Non-self-dual GL(n) | `wcrkqbzrj` | LANE CLOSES — three-way exhaustive collapse |

## The unifying theorem (verified core)
Every functional built from the completed zeta's **Klein four-group symmetry**
`{1, s↦1−s, s↦conj s, s↦1−conj s}` (plus flow-robust certificates) is **invariant under
`ρ↦1−conj(ρ)`**, so it is constant on the reflection pair `{ρ, 1−conj ρ}` (same ordinate,
mirrored real parts) — hence cannot separate an off-line zero from an on-line one, and
cannot force `Re ρ = ½`. **Kernel-verified core:**
`ReflectionForced.functional_constant_on_reflection_pair` +
`reflection_pair_offline_distinct` + the reflection-symmetry certificate family
(`OrdinateInsensitivity`: functional-equation reflection + conjugation), all 3-axiom clean.

## The last lane closes by an exhaustive trichotomy
Non-self-dual GL(n) (complex root numbers) was the one lane where conjugation does not
close the quadruple. It closes anyway: any functional there is even-kernel + odd-kernel,
evaluated per-zero or per-family, and **all three cells collapse**:
1. **per-zero on a single `L(s,π)` → GRH-for-π** (the functional equation gives a reflection
   axis, never a confinement; non-self-duality only changes what sits opposite);
2. **even kernel → self-dual** (contragredient-Satake `a_{π̃}(p^k)=conj a_π(p^k)` ⇒ `2 Re a_π`,
   sign-blind, = the already-closed Rankin-Selberg `π×π̃`);
3. **odd kernel (the sole asymmetry) → family average** (root number enters only after
   summation; one-/n-level density parametrize zeros as `½+iγ` and average — never per-zero).
No fourth cell. This re-instantiates the campaign's own theorem on GL(n): the non-symmetric
arithmetic (Satake/root-number data) enters only as averages, never per-zero.

## What this is, and is not
- **Is:** a verified-by-elimination, partially-kernel-formalized structural characterization
  of why RH resists every current class of unconditional approach — reflection invariance,
  with the sole escape being non-symmetric arithmetic entering non-robustly. Five independent
  directions, one conclusion, the core in the kernel.
- **Is not:** a proof of RH. `conjecture1_proved = False`. The FORCED-half is a genuine theorem
  (core built); the full "every per-zero reflection-breaker needs non-robust arithmetic" stays
  a well-supported heuristic, RH-adjacent, bounded below by the open Selberg-degree conjecture.

## The precise shape of the missing idea
The missing idea must **break the functional-equation reflection invariance per-zero**, using
arithmetic (non-symmetric) input in a way that is **not** an average / exceptional-set count —
the one thing no tool in five directions does. That is a precise, non-trivial, verified target,
earned by exhaustive honest elimination. `conjecture1_proved = False.`
