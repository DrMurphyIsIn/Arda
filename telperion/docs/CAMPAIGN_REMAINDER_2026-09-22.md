# Campaign remainder synthesis, 2026-09-22

**conjecture1_proved = False.** Nothing in this document, in any artifact it cites, or in any memo it links proves or advances the Riemann Hypothesis. The RH-equivalent nodes (`RH_conjecture`, `MM_zeta_comb_membership`) were out of scope for this run and were not touched.

Scope: the ten remaining nodes of the rh, mirrormere and anduril campaigns, worktree `/Users/peterwmurphy/arda-goal-weil` (branch `mm/gauss-window`). Every agent in the run was read-only on the registry and ran no git commands; the lead owns every commit, audit, link, deprecation and grant listed below. All paths are relative to `/Users/peterwmurphy/arda-goal-weil/telperion/` unless absolute.

Reading the tables: **category** is the triage verdict (grantable / provable / design / compute); **outcome** is what actually happened this run; a node is a **pass** only if a kernel-checked artifact exists on a built island, its statement is contained by the registry gate, and the independent readers did not refute it. An unbuilt island, a design memo, or a conditional reduction whose hypothesis is itself an unproved node is NOT a pass, and the tables say so.

## 1. rh campaign

| node | category | outcome | evidence path | next action for the lead |
|---|---|---|---|---|
| `RH_dbn_H0_eq_xi` (C2, H_0 = xi/8) | design | NOT PASSED. No proof artifact; the statement module is still the generated stub; dbn island was unbuilt at triage (bootstrapped later in the run by the C4 attempt via reflink of `li_positivity` packages). Statement re-verified numerically to 30 digits; five-module decomposition A-E written. | `docs/DESIGN_RH_dbn_H0_eq_xi_2026-09-22.md` | DEFER. (a) `mission audit RH_dbn_H0_eq_xi` with an independent auditor so the node can leave draft; (b) optionally `mission add` the four sub-nodes (Riemann integral, cos form, g-kernel identities, double IBP) and re-point `depends_on`; (c) staff modules A and C in parallel only after (a). No single-session attempt. |
| `RH_dbn_rh_iff_H0_real_zeros` (C4 bridge) | provable (conditional) | REDUCED, NOT PROVED. Module `DBNRealZerosIff.lean` proves `dbn_rh_iff_H0_real_zeros_of_H0_eq_xi (hC2)` with the registry proposition as conclusion and the C2 registry proposition as hypothesis; built (8723 jobs), 39-line axiom guard, all `[propext, Classical.choice, Quot.sound]`. Skeptic 1: not refuted as a conditional reduction. Skeptic 2: REFUTED AS A DISCHARGE (the unconditional registry theorem is not stated anywhere; `#check @dbn_rh_iff_H0_real_zeros` is an unknown identifier; gate `statement_matches` returns False). Both skeptics agree the bridge is sound and the node stays unproved. | `examples/dbn/lean/DBNRealZerosIff.lean`, `examples/dbn/lean/AxiomGuardDBN.lean`, `examples/dbn/lean/lakefile.toml` | COMMIT the three files (do not add `examples/dbn/lean/.lake`, it is gitignored, root-owned, 14G). Record as a conditional attempt (verdict Reduced, remaining obligation `DBN.H0EqXi` = C2). DO NOT GRANT and do not link as a proof. Resolve the doc conflict: file as ASCENT_PLAN F6-1 (bridge), correct WALL_BACKLOG_MAP row 50 (F0 IS-THE-WALL is wrong; the node is not RH-equivalent as a proposition). Closes by one line the day C2 lands. |
| `RH_dbn_debruijn_real_zeros` (C3, de Bruijn t >= 1/2) | design | NOT PASSED. No artifact. Route fixed as strip contraction (C2-free): H_0 zero strip from the Euler product half-plane, discrete de Bruijn step contracting the strip, Hurwitz. Requires even-order-<2 Hadamard factorisation absent from Mathlib (~1950 lines, critical path). | `docs/DESIGN_RH_dbn_debruijn_real_zeros_2026-09-22.md` | DEFER. Registry hygiene now: drop the non-load-bearing `depends_on` edge to `RH_dbn_H0_eq_xi`; `mission add` lemma node `RH_dbn_H0_zero_strip` (forall z, `DBN.H 0 z = 0 -> z.im ^ 2 <= 1`, ~950 lines, provable now) and point C3 at it; `mission audit` before any attempt. Not a one-session provable. |
| `RH_weil_window_floor_of_certified_block` (Zhu Thm 1.1) | design | NOT PASSED, UNPROVABLE BY CONSTRUCTION. All four analytic inputs are `opaque ... : Prop` in `RHDefs.lean`; a uniform proof would also prove a false statement (unconstrained lam0). `mission open-leaves rh` does not list it. Only two kernel-provable pieces exist (two-block Loewner floor eq. 13, Cholesky residual Lemma 5.2), 350-450 lines, and they do not close the node. Sibling `.lake` for weil_form_enclosure is a symlink into `~/arda-million` quasicrystal; do not `cp -Rc` the symlink. | `docs/DESIGN_RH_weil_window_floor_of_certified_block_2026-09-22.md` | DEFER (lead decision). Option 1 (recommended): freeze as documentary conditional node at draft, zero cost. Option 2: register `RH_block_floor_two_block` and `RH_block_floor_cholesky_residual` on weil_form_enclosure; node itself stays draft either way (ReducedHeadFloor is a permanent Arb trust seam). Never weaken the opaques. |

## 2. mirrormere campaign

| node | category | outcome | evidence path | next action for the lead |
|---|---|---|---|---|
| `MM_leakage_composite_zero` | provable -> PROVED | PASS. Gate mirror `MMLeakageStatement.leakage_composite_zero` appended to `LeakageNode.lean` (statement module's four inlined defs + theorem character-for-character, discharged by `Quasicrystal.leakage_composite_zero`). Built on the quasicrystal island (Mathlib 81a5d257, v4.32.0; 8660 jobs); `lake env lean AxiomGuardLeakage.lean` -> 48 lines, all `[propext, Classical.choice, Quot.sound]`, 0 sorryAx. Registry gate dry run: normalized statement contained at offset 1129/2114, markers []. Skeptic 1: NOT REFUTED (independent no-op rebuild, byte-identical guard output, mpmath kappa/b6 check, in-kernel probes). Skeptic 2: NOT REFUTED (independent rebuild, probe enumerating the mirror namespace, 50-digit numerics, `mission verify mirrormere` OK, mirror-drift tests 6 passed). Caveat both skeptics flagged: the island `.lake` here is a root-owned SYMLINK to `~/arda-million`'s build shared by seven worktrees, not a reflink; the oleans there were built from this worktree's sources (no sibling has the Leakage files). | `examples/quasicrystal/lean/LeakageNode.lean`, `examples/quasicrystal/lean/AxiomGuardLeakage.lean`; skeptic guard copies in scratchpad `qc_axioms.txt`, `skeptic2_axioms.txt` | COMMIT the two files. Then GRANT sequence: independent readback via `mission audit MM_leakage_composite_zero --auditor <independent>` (node is draft, readback None; grant_status refuses non-open nodes), then `mission grant MM_leakage_composite_zero --campaign mirrormere` (gate passes: contained, no markers, via=direct, depends_on = []). Optionally record a new attempt noting 48 guarded decls. Cosmetic follow-ups: guard `LeakageInstances.leak_dh_enclosure_decimal`; fix stale refs in `LeakageDictionary.lean` lines 7 and 198. |
| `MM_torus_section_dictionary` | grantable (mechanically) | ARTIFACT PASS, POLICY HOLD. Two independent blind re-audits: both `pass = true`, `axioms_clean = true`, `statement_byte_identical = true`; both rebuilt from source on a private reflink copy (no build through the shared symlink); both find the node is NOTATION ONLY (one `Fin.sum_univ_two` rewrite, then rfl) and both recommend DEPRECATE per WALL_BACKLOG_MAP R5, not promote+grant. The only failing grant gate is `status = draft`; `promote_to_open` would mechanically succeed. The stated reason to promote (unblocking `MM_torus_section_n2_rigidity`) lapsed on 2026-09-19 when that node was granted via direct. Deprecation flips no closure flag; `mission verify` stays green (simulated in memory). | `docs/AUDIT_TESTIMONY_MM_torus_section_dictionary_2026-09-22_A1.md`, `docs/AUDIT_TESTIMONY_MM_torus_section_dictionary_2026-09-22_A2.md`; probes under `examples/quasicrystal/lean/Probes/` and `missions/mirrormere/lean/Probes/` | DEPRECATE (recommended): `registry.deprecate("mirrormere", "MM_torus_section_dictionary", reason)` from Python (no CLI subcommand exists), reason "notation-only per 2026-09-19 and two 2026-09-22 blind read-backs; R5". Deprecate BEFORE running `mission audit` with either testimony, because `mission audit` has no hold flag and auto-promotes. Prune or annotate the documentary edges from `MM_torus_section_n2_rigidity` and `MM_zeta_comb_membership`. If the lead insists on the grant it will pass mechanically, against both auditors' advice. Commit the two testimonies; decide whether to keep the probe files. |
| `MM_speiser_box_probe` | design | NOT PASSED. Artifact honest and complete modulo N1 (`|zeta''| <= 3/10` on the box) and N2 (eight `|zeta'| >= 1/10` grid floors); 17 axiom-guarded decls, but island unbuilt anywhere (matching-rev Mathlib in `~/arda-gw-finite` zeta_reflection). Both hinted shortcuts REJECTED with numbers: LowHeightBox is order-0 EM on a different pin (~100x too weak, N ~ 10^9 terms); winding_box_zero is an Arb sidecar with zero kernel content. Route: derivative EM evaluator on zeta_reflection (Cauchy estimate B-ii recommended) + Taylor-cell instrument for N1 (16 cells at M = 3/8, N = 50, K = 5). | `docs/DESIGN_MM_speiser_box_probe_2026-09-22.md` | DEFER. Execute WALL_BACKLOG_MAP R4: `mission add MM_winding_second_derivative --kind lemma` (corrected title: derivative EM evaluator + Taylor-cell instrument), re-dep the probe on it, log this memo as a Stalled attempt. Optional cheap step: reflink `~/arda-gw-finite/.../zeta_reflection/lean/.lake/packages` into the probe island and rebuild (5-15 min) so a later audit has a live build. 2-4 week project; do not staff on the probe node itself. |

## 3. anduril campaign

| node | category | outcome | evidence path | next action for the lead |
|---|---|---|---|---|
| `AND_ladder_h280000` | design | NOT PASSED: REGISTRY-SHAPE MISMATCH. Registered statement is hypothesis-free; the island theorem `all_nontrivial_zeros_up_to_height_280000_of_bands` carries 280 `BandHyp` binders + `hgamma`. Zero of 642 band files discharge a `BandHyp`; every `RHInBoxT_*` module consumes `hLine` + `hArbT` Arb inputs. Substring gate tested in Python: fails on the artifact and on a same-name conditional wrapper. Built copy exists in `~/arda-million` (manifest matches, sources byte-identical) but not here. Corrections: 10,379 band modules (not 11,200), 20,758 Arb-class binders; `StripClear.height_floor_of_box_certs (T)` is already generic in T. | `docs/DESIGN_AND_ladder_h280000_2026-09-22.md` | DEFER (lead decision, ~1 page): re-register the statement in the Arb-conditional shape (A1 verbatim `_of_bands`, or A2 indexed `SegBandHyp` wrapper, recommended, ~350 generated lines, reusable for 1e6), then link `AllZeros_h280000.lean`, blind audit, grant on the `~/arda-million` build. Retitle node + readback: the binders are the Arb trust boundary, not glue; fix WALL_BACKLOG_MAP row 70. The hypothesis-free form is a multi-quarter kernel-interval-arithmetic program (route B) and belongs under `AND_ladder_1e9`. |
| `AND_ladder_1e6` | compute | NOT PASSED; JOB PLAN WRITTEN. Live state in `~/arda-million`: 27,503 ok bands / 0 refused, top 680000, leg 1 (640000-680000) emitted and at the lake-build step, 526 CPU-h spent. Remaining: 8 legs, 14,492 bands, ~700 CPU-h Arb driver + 9 serialized builds (~60-75 min each). Disk 42 GiB free; ~8 GB persistent + ~12 GB transient IR per leg. Two operational findings: `lake env` fails on the monolith (E2BIG reproduced), so `verify-bands` and the 59k-line guard are not runnable (per-leg `Guard_h<B>.lean` substitute); the B2 sharded packages are empty stubs. The 1e6 artifact will carry 1000 `BandHyp` binders + `hgamma`, so the grant gate is the same as h280000. | `docs/COMPUTE_PLAN_AND_ladder_1e6_2026-09-22.md` | RE-RUN (schedule compute, lead authorises): (a) authorise disk reclaim (`~/arda-b2` 18 GB superseded @560000 first); (b) run leg 1 build + IR prune in `~/arda-million`, then legs 2-9 under the prune-per-leg rule, never under 20 GiB free, one build per island machine-wide; (c) pick a verify-bands remedy (direct `lean` with LEAN_PATH or populated shard); (d) grant is impossible by compute alone; it needs the h280000 shape decision (G1-G3). |
| `AND_ladder_1e9` | design | NOT PASSED. Gated on the unauthored A3 Riemann-Siegel era (theta branch, RS main sum, Gabcke C0), no Lean prior art anywhere; T5/EM kernel economics end at t ~ 10^4 so wall-clock cannot close it. Memo designs the theta branch at the log-Gamma level (3-5 aw, staffable), RS main sum + Gabcke (11-21 aw, do-not-staff), flags an unnamed fourth brick (off-line horizontal-edge evaluator) and a suspect Gabcke literal in the charter (0.053 t^(-3/4) may mix K=1 constant with K=0 exponent). | `docs/DESIGN_AND_ladder_1e9_2026-09-22.md` | DEFER. Execute op R8: `mission add` draft nodes `AND_theta_branch`, `AND_rs_main_sum`, `AND_gabcke_c0`, add to `depends_on`; resolve the Gabcke constant against Gabcke 1979 / Arias de Reyna 2011 BEFORE generating any statement file; amend the node title (pure 3-axiom decide at 1e9 is out by 3-4 orders); log an attempt NoGo "design gate, A3 unauthored". Do not staff a proof agent. |
| `AND_ladder_1e13` | compute (charter-permanent goal) | NOT PASSED; HOLD. Two charter-tightening findings: the registered statement is at HEIGHT 10^13 = ~4.31e13 zeros (the "10^13 zeros" reading is a zero count at height ~2.445e12), lead decision D1; per-point RS cannot reach 10^13 at any budget (~1.1e7 core-years native), so G5 needs an unauthored verified FFT-amortised multieval (O4). Final axiom set would include `Lean.ofReduceBool`. | `docs/COMPUTE_PLAN_AND_ladder_1e13_2026-09-22.md` | DEFER indefinitely (WALL_BACKLOG_MAP 1.3 exemption). Lead-side only: D1 height-vs-count decision, retitle with zero count and expanded axiom set, R8 for 1e9. Only safe start: J0 native benchmark spike with an explicit lead exemption. Do not procure compute before an O4 design memo. |

## 4. Grants the lead may run now

Rule applied: a node is grant-ready only if its attempt was not refuted by either skeptic, or both blind audits pass, AND the artifact is built and kernel-checked, AND the registry gate is satisfied except for lead-owned procedural steps.

1. **`MM_leakage_composite_zero`** (mirrormere). GRANT-READY. Attempt Proved; neither skeptic refuted it; built and axiom-clean on the quasicrystal island (shared `~/arda-million` build, no-op rebuild reproduced independently twice); registry gate dry run passes. Procedural prerequisites, in order: commit `LeakageNode.lean` + `AxiomGuardLeakage.lean`; `mission audit MM_leakage_composite_zero` with an independent auditor (records readback, promotes draft -> open); `mission grant MM_leakage_composite_zero --campaign mirrormere`.

2. **`MM_torus_section_dictionary`** (mirrormere). MECHANICALLY GRANT-ELIGIBLE, RECOMMENDED AGAINST. Both audits pass the artifact; the only failing gate is `status = draft`. Both auditors, the 2026-09-19 readback, the node title and WALL_BACKLOG_MAP R5 all say deprecate rather than promote. The honest lead action is `registry.deprecate(...)` with reason; a grant would count a notation change as a proved node.

No other node is grant-ready. Explicitly NOT grant-ready despite kernel-clean Lean this run: `RH_dbn_rh_iff_H0_real_zeros` (conditional on `DBN.H0EqXi` = unproved C2; skeptic 2 refuted it as a discharge; commit and record as Reduced only).

Compact list: `MM_leakage_composite_zero` (grant after audit); `MM_torus_section_dictionary` (deprecate, not grant).

## 5. Open obligations ranked by value per line

Value = registry nodes closed or unblocked; lines = new handwritten Lean (generated lines noted separately). Lead-only registry operations cost zero lines and are listed first because they unblock everything below.

| rank | obligation | lines | value | notes |
|---|---|---|---|---|
| 0 | Registry hygiene batch (lead, 0 lines): audit+grant leakage; deprecate torus dictionary; fix WALL_BACKLOG_MAP rows 50 and 70; R4 (`MM_winding_second_derivative`), R8 (three A3 bricks), `RH_dbn_H0_zero_strip` node; drop C3 -> C2 edge; D1 for 1e13; readback corrections on h280000 | 0 | 1 grant, 1 deprecation, DAG made honest | Every later item is gated on at least one of these. |
| 1 | `AND_ladder_h280000` route A2: lead re-registers the statement with binders; agent emits the indexed `SegBandHyp` wrapper + `Guard_h280000` | ~350 generated + 7 | closes 1 node (finite verification to 280000, Arb-conditional) | Grantable in one session on the existing `~/arda-million` build; wrapper reusable for 1e6. |
| 2 | `RH_dbn_rh_iff_H0_real_zeros` final line once C2 lands | 1 | closes 1 node | Already written modulo `DBN.H0EqXi`. |
| 3 | `RH_dbn_H0_eq_xi` modules A-E | 1000-1550 | closes C2, closes C4 by rank 2, removes the only blocker on the C route | Modules A (Riemann integral from `WeakFEPair.Lambda0`) and C (g-kernel identities) independent; 4-6 sessions. Best analytic value per line in the run. |
| 4 | `RH_dbn_H0_zero_strip` (C2-free H_0 zero strip, new lemma node) | ~950 | closes 1 new node, first rung of C3 | Euler-product half-plane + reflection; no Hadamard needed. |
| 5 | `AND_ladder_1e6` compute (legs 1-9) | 0 Lean, ~700 CPU-h, ~10 h builds | reaches 10^6 but closes NOTHING without rank 1's shape decision | Disk-bound; verify-bands remedy needed. |
| 6 | `MM_speiser_box_probe` machinery (derivative EM evaluator B-ii + Taylor-cell instrument + forge extension) | 1600-2500 handwritten + ~230k generated + ~1000 py | closes 1 node | 2-4 weeks; not the Speiser wall. |
| 7 | `AND_theta_branch` (log-Gamma-level theta bound) | 2800-4900 | first A3 brick, pays off inside the EM era too | Staffable now; 3-5 aw. |
| 8 | `RH_dbn_debruijn_real_zeros` L1-L6 | 3500-6000 | closes C3 | Hadamard even-order-<2 factorisation (~1950 lines) is the critical path, 4-6 weeks; watch Mathlib ValueDistribution for an upstream theorem first. |
| 9 | `RH_weil_window` two-block + Cholesky lemmas (option 2) | 350-450 | closes 0 nodes | Node stays draft under both options; lowest value per line among cheap items. |
| 10 | `AND_rs_main_sum` + `AND_gabcke_c0` | 4800-9500 | second and third A3 bricks; prerequisite for 1e9 | Research summit, do-not-staff per ASCENT_PLAN F5-4. |
| 11 | `AND_ladder_1e9` climb D3 and `AND_ladder_1e13` O4 multieval | 8000-31000 + core-years | finite verification only | Not on any 12-24 month path. |

## 6. House-rule and hygiene ledger for this run

- No agent ran a git command or mutated the registry; every registry write above is the lead's.
- Builds: dbn island bootstrapped by reflink of `li_positivity/.lake/packages` (two builds, serialized); quasicrystal built once through the shared `~/arda-million` symlink (both skeptics ran only no-op rebuilds); the torus audits built in private scratch copies since deleted. No other island was built. Two `.lake` trees in this worktree are root-owned; the user's own rebuilds may need a chown.
- Cross-worktree hazard recorded by both leakage skeptics: all seven quasicrystal `.lake` directories on this machine resolve to one build directory, so "one build per island" must be enforced machine-wide.
- Every touched Lean file and every memo states conjecture1_proved = False and that nothing proves RH; CI word scans and emoji scans were run clean on every touched file.
- Nothing in this run bears on the truth of RH. conjecture1_proved = False.
